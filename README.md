# Tapio

منصة خفيفة لإنشاء **أسئلة تفاعلية واستطلاعات حية** — على غرار Mentimeter — مصمّمة للمدرب أو المعلم:
يجهّز أسئلته، يعرض رمز QR، فيدخل المتدربون من جوالاتهم ويجيبون مباشرة، بينما يتابع هو التقدّم والإحصاءات على لوحة تحكم حيّة.

> **نتائج المشاركين مؤقتة بالكامل.** إجابات الطلاب ونقاطهم في ذاكرة الخادم (RAM) فقط، وتُحذف
> تلقائياً بعد ٣٠ دقيقة من إنهاء الجلسة، أو بعد ٣ ساعات من الخمول، أو فور إعادة تشغيل الخادم.
> الدائم — وباختيار المدرب — هو **حسابه وأسئلته المحفوظة**، وهي محتواه هو لا بيانات طلابه.
>
> ويُحفظ معها **هيكل الجلسة الحيّة** (الرمز والأسئلة والإعدادات وموضع العرض) كي ينجو رمزٌ
> وُزِّع على صفّ من إعادة نشرٍ أو إعادة تشغيل — [التفاصيل](#بقاء-الجلسات-بعد-إعادة-التشغيل-).
> لا اسم مشارك ولا إجابة ولا درجة تلمس القرص في أي حال.

### اللغة 🌐

التطبيق **ثنائي اللغة بالكامل** (عربي/إنجليزي): الصفحة الرئيسية، دخول المدرب، لوحة المعلّم،
المحرّر، مساعد الذكاء الاصطناعي، لوحة التحليلات، شاشة المشارك، شاشة البروجكتر، وتقارير
PDF/Excel. تُكتشف لغة المتصفح تلقائياً (عربي ⇄ عربي، أي شيء آخر ⇄ إنجليزي) مع زر تبديل يدوي
يحفظ الاختيار، واتجاه الصفحة (RTL/LTR) يتبدّل معها قبل أي رسم.

**لغة النشاط تتبع منشئه:** حين يطلق المعلّم نشاطاً تُحفظ لغته الحالية مع النشاط، فيرى كل مشارك
النشاط بلغة صاحبه مهما كانت لغة متصفحه — نشاط إنجليزي يظهر بالإنجليزية وLTR حتى على متصفح عربي،
والعكس. وهذا لا يغيّر تفضيل الطالب المحفوظ لبقية الموقع.

**لا شيء في التطبيق بالعربية وحدها بعد الآن** — بما في ذلك **دليل المعلّم** المطوّل.

---

## التشغيل

```bash
npm install
npm start          # http://localhost:3000
```

> **مهم:** التطبيق يحتاج خادم Node يعمل. فتح ملفات `public/*.html` مباشرة من المجلد (بروتوكول `file://`)
> أو من خادم ملفات ثابت لن ينجح: ستظهر الصفحات لكن إنشاء الجلسات والاتصال المباشر لن يعملا.
> في هذه الحالة يعرض التطبيق شريطاً تحذيرياً يشرح الحل بدل رسالة خطأ غامضة.
> القوالب الجاهزة تعمل دائماً لأنها محمّلة مع الصفحة بلا أي طلب شبكة.

للتطوير مع إعادة التحميل التلقائي:

```bash
npm run dev
```

الاختبارات (تكامل حقيقي عبر HTTP + WebSocket):

```bash
npm test
```

متغيرات البيئة: `PORT` (افتراضياً `3000`) — والتشغيل المحلي لا يحتاج غيره.
الجدول الكامل لمتغيرات الإنتاج (قاعدة البيانات، دخول جوجل، الذكاء الاصطناعي، المالك)
في [إعدادات Render](#٢-متغيرات-البيئة-الموصى-بها).

---

## النشر على الإنترنت

التطبيق يحتاج **عملية Node دائمة** لسببين: خادم WebSocket للاتصال المباشر، وحالة الجلسة في ذاكرة العملية.

### ✅ منصات تعمل (بلا أي تعديل)

| المنصة | الطريقة |
| --- | --- |
| **Render** | اربط المستودع — ملف `render.yaml` جاهز ([التفاصيل أدناه](#إعدادات-render-بالتفصيل)) |
| **Railway** | اربط المستودع؛ يكتشف `npm start` تلقائياً |
| **Fly.io** | `fly launch` ثم `fly deploy` — يستخدم `Dockerfile` الجاهز |
| **Koyeb / Cloud Run / VPS** | من `Dockerfile` مباشرة، أو `npm ci && npm start` خلف Nginx |

المنصة تحقن `PORT` تلقائياً والتطبيق يلتزم به. لا حاجة لأي قاعدة بيانات أو متغيرات أخرى.

### ❌ منصات لا تصلح

**Vercel · Netlify · GitHub Pages · Cloudflare Pages** (بوضعها الثابت/serverless):

- لا تستضيف خادم WebSocket، فالاتصال المباشر لن يعمل.
- الدوالّ serverless قصيرة العمر ولا تتشارك الذاكرة، فحالة الجلسة تضيع بين الطلبات.

النتيجة عليها: **تظهر الصفحات وتفشل كل طلبات `/api`** — القوالب تعمل (لأنها محمّلة مع الصفحة)
لكن إنشاء جلسة يفشل. التطبيق يكتشف هذه الحالة ويعرض شريطاً يشرح السبب بدل رسالة خطأ غامضة.

> لتشغيله على Vercel يلزم استبدال طبقة الاتصال المباشر بخدمة خارجية (Ably/Pusher) ونقل الحالة إلى مخزن
> خارجي مثل Redis — وهي إعادة بناء لجوهر التطبيق، لذلك التوصية هي استخدام إحدى المنصات أعلاه.

---

## إعدادات Render بالتفصيل

### ١. نوع الخدمة (الأهم)

اختر **Web Service** وليس Static Site. الفرق حاسم:
Static Site تخدم ملفات فقط، فتظهر صفحات التطبيق لكن كل `/api` يفشل ولا يعمل أي اتصال مباشر.

| الحقل | القيمة |
| --- | --- |
| Type | **Web Service** |
| Runtime | Node |
| Build Command | `npm ci` |
| Start Command | `npm start` |
| Health Check Path | `/api/health` |

لا تضبط `PORT` يدوياً — Render يحقنه والتطبيق يلتزم به.

### ٢. متغيرات البيئة

**لازمة للإنتاج:**

| المتغير | القيمة | لماذا هي لازمة |
| --- | --- | --- |
| `DATABASE_URL` | رابط Postgres | بدونها تُكتب الحسابات في ملف على قرص مؤقت فتضيع مع كل نشر. استخدم **session pooler** من Supabase لا العنوان المباشر `db.*.supabase.co` |
| `GOOGLE_CLIENT_ID` | من Google Cloud | بدونها **لا يعمل تسجيل الدخول إطلاقاً** — وهو وسيلة الدخول الوحيدة |
| `GOOGLE_CLIENT_SECRET` | من Google Cloud | كما سبق |
| `ADMIN_EMAILS` | بريدك | يحدّد صاحب لوحة المالك. له قيمة افتراضية في الكود، لكن ضبطه صراحةً يجعل الصلاحية ظاهرة في لوحة Render لا مدفونة في المصدر |

**اختيارية (لها قيم افتراضية آمنة):**

| المتغير | الافتراضي | الأثر |
| --- | --- | --- |
| `NODE_ENV` | — | `production` وضع الإنتاج (يكتم سجلّ الاختبارات فقط؛ التخزين المؤقت مضبوط في الكود لا به) |
| `NODE_VERSION` | افتراضي المنصة | مثبّت على `22` في `render.yaml` |
| `KEEP_ALIVE_MINUTES` | `10` | نبضة تمنع نوم الخدمة أثناء المحاضرة |
| `SESSION_IDLE_MINUTES` | `180` | مدة بقاء الجلسة بلا نشاط |
| `SESSION_ENDED_MINUTES` | `30` | مدة بقاء نتائج جلسة انتهت |
| `DATA_DIR` | `./data` | مجلد ملف التخزين حين لا توجد `DATABASE_URL` |
| `KEEP_ALIVE_URL` | `RENDER_EXTERNAL_URL` | عنوان النبضة إن لم تحقنه المنصة |
| `PREMIUM_WHATSAPP` | `970597034066` | رقم التواصل في نافذة الاشتراك |
| `PREMIUM_PRICE_USD` | `5` | السعر المعروض |
| `PREMIUM_SIGNUP_TRIAL_DAYS` | `10` | أيام بريميوم تُمنح تلقائياً لكل حساب جديد — صفرٌ يوقف المنحة |

**للمساعد الذكي (اختياري — بدونها يردّ المساعد ٥٠٣ ويبقى باقي التطبيق سليماً):**

| المتغير | الافتراضي | الأثر |
| --- | --- | --- |
| `AZURE_OPENAI_KEY` | — | المفتاح. يُقبل كذلك الاسم البديل `AZURE_OPENAI_API_KEY` |
| `AZURE_OPENAI_ENDPOINT` | مورد makeflow | **اضبطه صراحةً** مع أي مفتاح جديد، وإلا ذهبت الطلبات إلى المورد الافتراضي |
| `AZURE_OPENAI_MODEL` | `gpt-4.1` | اسم النشر |
| `AZURE_OPENAI_API_VERSION` | — | لمسار قديم لا يستخدم `/openai/v1/` |

> **لا يوجد أي سرّ ذي قيمة افتراضية في الكود** — المفاتيح كلها تبدأ فارغة، والميزة التي ينقصها مفتاحها تتعطّل وحدها بلا أن تعمل بمفتاح خاطئ.

الاختيارية كلها موجودة في `render.yaml`، فإن ربطت المستودع كـ Blueprint تُضبط تلقائياً.
اللازمة تضبطها بنفسك في لوحة Render لأنها أسرار.

### ٣. مشكلة النوم بعد ١٥ دقيقة (وحلّها)

الخطة المجانية على Render **توقف الخدمة بعد ١٥ دقيقة بلا طلبات واردة**، وإيقافها يمسح إجابات
الطلاب لأنها في الذاكرة (الرمز والأسئلة يعودان — [انظر](#بقاء-الجلسات-بعد-إعادة-التشغيل-)).
هذا يعني أن ما أجابه طلابك في أول المحاضرة قد يضيع في وسطها.

**الحل المدمج:** الخادم يرسل لنفسه نبضة خفيفة كل ١٠ دقائق عبر عنوانه العام
(`RENDER_EXTERNAL_URL` الذي يضبطه Render تلقائياً) — **لكن فقط ما دامت هناك جلسة قائمة**،
حتى لا تُستهلك ساعات الخطة المجانية بلا داعٍ. فما دام لديك نشاط مفتوح، لن تنام الخدمة.

للتحقق من أنها مفعّلة، افتح `https://<موقعك>/api/health` وابحث عن:

```json
{ "ok": true, "config": { "keepAlive": "https://<موقعك>", "keepAliveMinutes": 10 } }
```

إن كانت `keepAlive` تساوي `null` فالنبضة معطّلة — أضف `KEEP_ALIVE_URL` بعنوان موقعك يدوياً.

> الخطة المجانية تمنح ٧٥٠ ساعة شهرياً. النبضة تعمل أثناء الجلسات فقط، لكن إن كنت تستخدم
> التطبيق يومياً وبكثافة ففكّر في أرخص خطة مدفوعة — فهي لا تنام أصلاً وتزول المشكلة تماماً.

### ٤. حلّ مشكلة «Not Found»

هذه الكلمة الإنجليزية **لا يُخرجها التطبيق إطلاقاً** — كل رسائله عربية، وأي مسار مجهول يعيد
الصفحة الرئيسية، ورابط الدخول `/j/<الرمز>` يحوّل دائماً إلى صفحة المشاركة. فإن رأيتها فهي من Render.

للتشخيص في ثانية واحدة:

```bash
curl -I https://<موقعك>/api/health
```

| ما تراه | المعنى | العلاج |
| --- | --- | --- |
| ترويسة `x-render-routing: no-server` | لا توجد خدمة تعمل على هذا العنوان | الخدمة موقوفة أو فشل نشرها أو نوعها Static Site |
| `404` وردّ HTML من Render | غالباً Static Site | أعد إنشاءها كـ Web Service |
| `200` مع `{"ok":true}` | الخادم يعمل | المشكلة في الجلسة نفسها لا في النشر |

وإذا كان الخادم يعمل لكن الرابط لا يفتح جلستك، فالسبب أن الجلسة تجاوزت مهلة الخمول (٣ ساعات)
أو أُنهيت ومضى عليها ٣٠ دقيقة. في هذه الحالة تظهر للمتدرب رسالة عربية واضحة «لم نعثر على الجلسة»
— أنشئ جلسة جديدة واعرض رمزها.

> **تذكّر:** إعادة النشر لم تعد تمسح رمزك — [هيكل الجلسة محفوظ](#بقاء-الجلسات-بعد-إعادة-التشغيل-)
> والجلسة تعود وحدها عند الإقلاع. لكن **إجابات ما قبل إعادة التشغيل لا تعود** (فهي في الذاكرة
> عمداً)، فتجنّب النشر أثناء محاضرة جارية رغم ذلك.

### شرائح المحتوى 🖼️

نوع `slide`: عنوان ونصّ شرح (وصورة اختيارية) **تُعرض ولا تُجاب** — فيشرح المعلّم ويسأل في تدفّق
واحد بدل التنقّل بين برنامج عرض والتطبيق.

تعيش الشريحة داخل مصفوفة الأسئلة نفسها كي ترث التنقّل والجدولة والبثّ بلا مسار موازٍ، لكنها
**تُستثنى من كل ما يخصّ التقييم**: لا علامة، ولا تُحسب في تقدّم الطالب (`answerableUpTo`)، ولا
في نسبة المشاركة، ولا في الأوسمة، ولا سطر لها في التقرير أو التحليل. وشريط المدرب عليها يعرض
زرّ انتقال واحد فقط — لا «إغلاق الإجابة» ولا «عرض النتائج» لشيء لا يُجاب.

### استيراد بنك الأسئلة من جدول 📥

`assets/js/import-sheet.js`: يقبل CSV/TSV من Excel أو Google Sheets، أو **لصقاً مباشراً** من
الجدول. الهدف ليس صيغة مثالية بل قبول ما يكتبه المعلّمون فعلاً:

- **الفاصل يُكتشف تلقائياً**: فاصلة أو **فاصلة منقوطة** (تصدير Excel العربي) أو تبويب. تخمينه
  خطأً يجعل كل صفّ عموداً واحداً، فنختار الأكثر تكراراً في الترويسة.
- **علامة ترتيب البايت (BOM)** تُزال، والنموذج المُنزَّل يحملها كي يفتحه Excel بالعربية لا برموز.
- **العناوين عربية أو إنجليزية** بمرادفاتها الشائعة.
- **الإجابة الصحيحة** تُفهم نصاً حرفياً، أو حرفاً (`A` / `أ`)، أو رقماً (`3`)، أو عدّة إجابات مفصولة.
- **بلا صفّ عناوين** يُفترض الترتيب الشائع: السؤال ثم الخيارات ثم الإجابة في آخر عمود.
- **النوع يُستنتج** حين لا يُذكر: خياران فأكثر مع إجابة = اختيار من متعدد، بلا إجابة = استطلاع،
  `صح/خطأ` = صح وخطأ، ووجود `___` = أكمل الفراغ.
- صفّ لا تُفهم إجابته يصير **استطلاعاً بتنبيه** بدل أن نخترع له جواباً.

التحليل كله في المتصفح — **ملف المعلّم لا يُرفع إلى أي خادم**.

### أنواع الأسئلة العشرة ونزاهة الاختبار 🔢

بعد `order` و`match` صارت الأنواع تسعة، ونوعان منها **يُصحَّحان آلياً بعلامة جزئية** — وهو ما
تفتقده أغلب المنصات: من رتّب ثلاثة من أربعة صحيحاً يأخذ ٧٥٪ لا صفراً.

| النوع | التصحيح | الفكرة |
| --- | --- | --- |
| 🔢 **رتّب** (`order`) | آلي، جزئي | المعلّم يكتب العناصر بالترتيب الصحيح، والتطبيق يخلطها لكل طالب. النسبة = كم عنصراً وقع في موضعه |
| 🔗 **طابِق** (`match`) | آلي، جزئي | أزواج (طرف ⟷ ما يقابله)؛ الأطراف اليمنى تُخلط وتصير قائمة اختيار لكل طرف أيسر. النسبة = كم زوجاً صحّ |

**واجهة الطالب بالنقر لا بالسحب**: السحب على الجوال هشّ — يتعارك مع تمرير الصفحة ومع قارئ
الشاشة. الترتيب بالنقر بالتسلسل (ونقرة ثانية تُلغي)، والمطابقة بقائمة اختيار. يعمل بإصبع واحدة
وبلوحة مفاتيح.

**نتائج البروجكتر** لهذين النوعين تعرض ما يفيد المعلّم فعلاً: متوسط الإتقان، وكم أتقنه كاملاً،
و**نسبة كل عنصر أو زوج على حدة** — فيعرف أي خطوة تحديداً تعثّر فيها الصف.

#### نزاهة الاختبار

| الإعداد | ما يفعل |
| --- | --- |
| **خلط ترتيب الأسئلة** | ترتيب مختلف في كل إطلاق، فلا يفيد أن يعرف طلاب الحصة التالية التسلسل |
| **خلط الخيارات لكل طالب** | ترتيب الخيارات يختلف من جهاز لآخر، فـ«ب» عند جارك ليست «ب» عندك |

الخلط **مبذور بمعرّف الطالب** لا عشوائياً في كل رسم: الطالب الذي يفقد الاتصال ويعود يرى الترتيب
نفسه، وإلا لأجاب على خيار غير الذي رآه.

### قسم الألعاب التفاعلية 🎮

المعلّم يرفع لعبته (ملف HTML واحد مكتفٍ بذاته) باسمها و**صورةٍ مصغّرة** تدلّ
عليها، ويختار المادة من قائمة ثابتة والصفوف من شرائح تُنقر (واحد أو أكثر، أو
«كل المراحل»). والطلاب يبحثون ويلعبون في المتصفّح مباشرةً بلا حساب. الترتيب
بالأكثر لعباً أو الأعلى تقييماً أو الأحدث، والتصفية بالمادة والصف.

**اللعب ملء الشاشة لا داخل مربّع.** بطاقةُ اللعبة تعرض صورتها ومعلوماتها وزرّ
«▶ العب الآن»، وعنده تغطّي اللعبة الشاشةَ كاملةً بلا شريطٍ ولا تمرير — تتصرّف في
الجوال كتطبيق. الإطار باقٍ لأنه **هو** الحماية، لكنه لم يعد مرئيّاً كإطار.
والخروج بزرّ ✕ أو بزرّ الرجوع، لأن اللعب مسارٌ مستقلّ (`#/g/:id/play`) لا حالةٌ
داخلية.

**روابط تُشارَك.** لكل لعبة رابطها (`#/g/:id`)، ولكل معلّم رابطُ صفحة ألعابه
(`#/t/:teacherId`) ينسخه من لوحته ويضعه في مجموعة صفّه، فيتصفّح طلابه ألعابه
وحدها. واسم المعلّم في بطاقة أي لعبة يوصل إلى الصفحة نفسها. ومن لا يملك
الرابط يصل من **دليل المعلّمين** (`#/teachers`) — كل معلّم بعدد ألعابه.

**العلامات ليست النقاط — ولكلٍّ لغته.**

| | العلامات 📝 | النقاط 🎮 |
| --- | --- | --- |
| ما يضبطه المعلّم | العلامة الكاملة (١٠، ٢٠، ٣٠…) | نقاط الجائزة تحت كل سؤال |
| التوزيع | بالتساوي (٣٠ ÷ ١٠ أسئلة = ٣) أو علامةٌ لكل سؤال مجموعها = الكاملة | لا توزيع — كل سؤال بنقاطه |
| أثر السرعة | **لا شيء إطلاقاً** | النقاط تتناقص مع ثواني الإجابة |
| ما يُعرض تحت السؤال | حقل العلامة (في التوزيع المخصّص وحده) | حقل النقاط |

`markShare(q)` هي الموضع الوحيد الذي يقرّر نصيب السؤال، و`markFor` تبني عليها.
وتوزيعٌ مخصّص لم تُملأ أرقامه يرجع للتساوي بدل أن يصفّر الجميع. والمحرّر يعرض
المجموع حيّاً ويحذّر ما دام يخالف العلامة الكاملة — «قبل الإطلاق» كما ينبغي.

**النافذة الزمنية اختيارية، ووضعان لا ثلاثة**: `none` (بلا مؤقّت —
**الافتراضي**) و`all` (ثوانٍ واحدة تسري على كل الأسئلة). ولا مؤقّت لكل سؤال
على حدة: كان يُثقل المحرّر برقمٍ تحت كل سؤال مقابل فائدةٍ نادرة، والاختبارُ
الذي يحتاج وقتاً يحتاجه لأسئلته كلها.

و`timeFor(q)` هي **الموضع الوحيد** الذي يُقرأ منه الوقت في الجلسة كلها —
قراءةُ `q.timeLimit` مباشرةً في موضعٍ منسيّ تعني مؤقّتاً يخالف اختيار
المعلّم، وقد كانت مبعثرةً في ستة مواضع قبل هذا.

ونشاطٌ قديم كان مؤقّتاً لا يفقد وقته صامتاً: `inferLegacyTime` تستنتج له
`all` **بأطول مهلةٍ** كانت فيه، فلا يُقصَّر على طالبٍ وقتُه.

**لا انزياح أفقي على الجوال — وهذا مفحوص.** عنصرُ الشبكة أو الفليكس حدُّه
الأدنى `auto`، أي عرض محتواه الأدنى: فسطرٌ لا يُكسر (رابطٌ طويل) أو جدولٌ من
خمسة أعمدة عربية يوسّع عمودَه ثم بطاقته ثم المستند كلَّه، فتنزاح الصفحة
أفقياً ويختفي نصفها. القاعدة: `min-width: 0` على أبناء `.stack`، والتمرير
يُطلَب صراحةً لمن يحتاجه (`.chart-card`, `.link-box`) — لا `overflow-x: hidden`
على الجسم، فذاك يُخفي العطل ولا يصلحه.

و`verify-mobile.js` يقيس `documentElement.scrollWidth` على شاشة ٣٩٠ في كل
صفحة وفي لوحة التحكم والتحليل، ويتحقّق أن كل جدولٍ أعرض من بطاقته يمرّر
نفسه داخلها.

**إطلاق نشاط يتطلّب حساباً.** كان `POST /api/sessions` مفتوحاً لأي زائر،
فيستطيع أي أحد أن يطلق نشاطاً على المنصة بلا أثرٍ يُنسب إليه — ولا إشراف على
ما يُعرض على الطلاب، ولا نشاطٌ يُحفظ لصاحبه. صار خلف `auth.requireUser`،
والواجهة تسوق من لا حساب له إلى الدخول بجوجل وتعيده إلى محرّره (وكذلك زرّ
التجربة السريعة) بدل أن ترميه برسالة خطأ.

**مسودة المساعد الذكي تفتح على المراجعة مباشرةً.** كان المعلّم يمرّ على
الإعدادات ثم على كل سؤالٍ ضغطةً ضغطة ليصل إلى زرّ الإطلاق، وقد صاغها المساعد
له كاملةً. `mount(root, onLaunch, extra, { startStage: 'review' })`.

**الواجهة لا تسأل الزائر عن رمز.** كان حقلُ رمز الدخول أوّلَ ما يقع عليه
البصر في الصفحة الرئيسية، فيظنّ من جاء يتصفّح أن عليه إدخال شيء ليدخل.
انتقل إلى صفحته المستقلّة على **`/c`** — عنوانٌ قصير يُملى على الطلاب شفهياً —
وبقي في الواجهة سطرٌ هادئ: «معك رمز دخول لنشاط؟».

والروابط القديمة كلّها تبقى تعمل: `‎/j/:code‎` (رمز QR) كما كان، و`‎/c/:code‎`
يمضي مباشرةً إلى النشاط، و`‎/?code=‎` يحوّل بدل أن يُبقي حقلاً.

**وزرّ دخول المعلّم صار بارزاً**: ملوّناً في أعلى الصفحة فوق الطيّة، وأوّلَ
زرٍّ في بطاقة المعلّم لا خامسَ خمسةٍ شاحبة. معلّمون كثيرون لم يجدوه.

**المحرّر معالجٌ من ثلاث مراحل**: `settings` ← `questions` ← `review`.

1. **الإعدادات** — القوالب والاستيراد ومعلومات النشاط وسيره ونظام التقييم
   والوقت. ولا سؤال على الشاشة هنا.
2. **الأسئلة** — سؤالٌ واحد في كل صفحة، وشريطُ أرقامٍ يُنقر للقفز (والمكتوب
   منها بعلامة خضراء)، و«السابق/التالي»، وعلى آخر سؤال «＋ سؤال جديد» أو
   «المراجعة والإطلاق».
3. **المراجعة** — كل الأسئلة في نظرة واحدة، والفارغُ منها بلونٍ محذّر، والنقر
   على أيٍّ منها يعيد المعلّم إلى صفحته. وزرُّ الإطلاق هنا وحده.

الغرض واحد: ألّا يرى المعلّم في الشاشة إلا ما يقرّره الآن. كانت الصفحة تعرض
كل شيء دفعةً واحدة — قوالبَ وإعداداتٍ وعشرين سؤالاً وإطلاقاً — فتصير جداراً
يُربك لا يُعين.

**والتقدّم الحرّ هو الافتراضي دائماً** — في المحرّر وفي القوالب الجاهزة وفي
مسودات المساعد الذكي معاً، لا في `normalizeSettings` وحدها.

**بروفايل المعلّم — اختياريٌّ بالكامل.** ثلاثة حقول يملؤها إن شاء: اسمٌ يظهر
للطلاب بدل اسم حساب جوجل، وصورة، ورقمٌ للتواصل بزرّ واتساب. من لم يملأ شيئاً
تبقى صفحته كما كانت تماماً — الاسم الأول وحده — فالميزة إضافةٌ لا شرط.

قاعدة الاسم: `publicNameOf` تعيد ما اختاره المعلّم، وإلا الاسم الأول. وهذا
مقصود — الأصل ألّا يُنشر إلا الاسم الأول، لكنّ من ملأ بروفايله اختار بنفسه
ما يُعرض. والمسار العلني `/api/teachers/:id` لا يعيد بريداً ولا معرّف جوجل
ولا حالة اشتراك: ما لم يكتبه المعلّم بيده لا يخرج منه.

الصورة تُصغَّر إلى ٢٥٦×٢٥٦ في جهازه، وتُقدَّم من `/api/teachers/:id/photo`.
و`findUserById` — التي تُنادى مع **كل طلبٍ مُصادق** — تختار أعمدتها صراحةً
وتستثني الصورة، وإلّا لَحُمِّل كل طلبٍ في المنصّة بـ`data URI` بلا داعٍ.

**سِمَة داكنة** للقسم: `html.dark` اسمٌ ثانٍ لرموز `html.stage` اللونية لا
نسخةٌ منها، فلا يوجد لونان يفترقان مع الوقت. الافتراضي يتبع نظام الزائر،
وزرُّ التبديل يثبّت اختياره. وخلفيةُ إطار اللعبة تبقى بيضاء في السِمَتين
عمداً: اللعبة محتوى طرفٍ ثالث كُتب على افتراض خلفيةٍ فاتحة.

**كل المصغّرات بمقاسٍ واحد** على العرض: الرفع يوحّدها إلى ٦٤٠×٣٦٠، والعرض
يضعها في صندوقٍ بنسبةٍ ثابتة تملؤه الصورة قصّاً لا تشويهاً، وصفوفُ الشبكة
متساوية الارتفاع.

**المواد والصفوف تُخزَّن كمعرِّفات** (`math`, `g3`) لا كنصوص: البحث والتصفية
يعملان بلغةٍ واحدة، بينما يقرأ كلُّ مستخدمٍ اسم المادة بلغته. وقيمةٌ حرّة من
رفعٍ قديم تُعرض كما كُتبت بلا كسر.

**الصورة المصغّرة** تُصغَّر في جهاز المعلّم إلى ٦٤٠×٣٦٠ قبل الرفع (صورةُ الجوال
بأربعة ميغابايت تصير عشراتِ الكيلوبايت)، وتُقدَّم من مسارها الخاص
`/api/games/:id/cover` بتخزينٍ ليوم — لا داخل JSON القائمة، فصفحةٌ من ٢٤ لعبة
تبقى كيلوبايتاتٍ لا ميغابايتات. الأنواع المقبولة PNG وJPEG وWebP وGIF فقط:
SVG مستندٌ كامل لا صورة، ولا داعي لفتح ذلك الباب من أجل بطاقة.

**تبديل صورة لعبةٍ مرفوعة** له مسارُه وحده `PATCH /api/games/:id/cover` (لصاحبها
لا لغيره): لعبةٌ بحجمها الأقصى ملفان ميغابايت، وإرسالها كلها لتبديل صورةٍ بحجم
عشرات الكيلوبايت هدرٌ ومخاطرة — أي عطبٍ في الرفع كان سيستبدل اللعبة لا صورتها.
ولأن مسار الصورة مخزَّنٌ في المتصفّح يوماً كاملاً، تحمل البطاقة `coverAt` (وهو
`updatedAt`) وتُلحقه الواجهة ببصمةٍ في العنوان — بدونها يبقى المعلّم يرى صورته
القديمة بعد أن بدّلها.

**نموذج الأمان هو المعمار كلّه.** نحن نستضيف شفرةً كتبها شخصٌ آخر ونشغّلها
لطلاب لا يعرفونه. لو قُدِّمت هذه الشفرة على نطاقنا بلا عزل لقرأت كوكي جلسة كل
من يفتحها، ونادت `/api` باسمه، وحذفت أنشطته. فالحماية في أربع طبقات:

1. **إطار معزول بلا `allow-same-origin`** — وهذه هي الضمانة الأساسية. أصل
   اللعبة يصير «مبهماً» (`origin === null`)، فلا كوكي ولا `localStorage` ولا
   وصول إلى الصفحة الأمّ ولا طلبات مُعتمَدة. ولا `allow-popups` ولا تنقّل
   للصفحة الأمّ، فلا نافذة تصيّد ولا تحويل.
2. **`Content-Security-Policy` على المستند نفسه**: `sandbox` في الترويسة — وهذا
   ما يجعل فتحَ المستند في تبويبٍ مستقلّ آمناً كذلك، وهو **مقيسٌ** لا مفترض:
   الفحص يفتح `/api/games/:id/frame` مباشرةً فيرصد `origin=null` و`api=BLOCKED`.
   ومعه `connect-src 'none'` و`form-action 'none'`
   فلا يغادر اللعبةَ شيءٌ يكتبه الطالب فيها، و`frame-src`/`object-src` ممنوعان.
3. **لا تنقية للشفرة إطلاقاً.** تنقيةُ HTML عامٍّ وهمُ أمانٍ: تكسر الألعاب
   الحقيقية ولا تصمد أمام محترف. العزل يمنع الضرر، فلا حاجة لادّعاء التنقية.
4. **إشراف**: المالك يحذف أي لعبة، وفي كل صفحة رابط إبلاغ.

مقيسٌ لا مُدَّعى: في `verify-games.js` لعبةٌ عدوانية تحاول القراءة والاتصال،
والنتيجة المرصودة `cookie=THREW | parent=THREW | storage=THREW | origin=null |
api=BLOCKED` — داخل الإطار وفي تبويبٍ مستقلّ سواء. و١٣ اختبار وحدة في
`test/games.test.js` تثبّت الترويسات والحدود وصحّة الصورة.

**ما يبقى مكشوفاً وأقوله صراحةً**: `img-src`/`script-src` يسمحان بـ`https:` كي
تعمل الألعاب التي تحمّل خطاً أو مكتبةً من CDN. فلعبةٌ خبيثة تستطيع نظريّاً
تسريب ما يكتبه الطالب **داخلها هي** عبر رابط صورة. ولا تصل إلى شيء آخر:
لا حساب، ولا نتائج، ولا بيانات المنصة. ومن أراد إغلاق هذا الباب أيضاً فالسطر
واحد: قصر `img-src` و`script-src` على `data: blob:`.

**اللعب بلا إنترنت — بالحفظ لا بالتنزيل.** زرٌّ في صفحة اللعبة يحفظها على
جهاز الطالب فتعمل مقطوعةَ الشبكة. اخترنا هذا على **تنزيل الملف** عمداً:
المُنزَّل يخرج من عزلنا (تسقط ترويسة CSP فتصير صفحةً كاملة الصلاحيات على جهاز
الطالب) ولا يمكن سحبُه لو تبيّن أن اللعبة مخالفة. أما المحفوظ فيبقى داخل
الإطار المعزول نفسه — وهذا **مقيس**: `Cache API` تحفظ الاستجابة بترويساتها
فتعود `sandbox` معها، وفي `verify-games.js` نقطع الشبكة تماماً ثم نلعب ونرصد
`origin=null`. وصاحب اللعبة وحده يقرّر: خانةٌ في نموذج الرفع تمنع الحفظ.

هذا أول استثناءٍ لقاعدة «عامل الخدمة لا يخزّن شيئاً»، وهو مضبوط: لا يُحفظ إلا
بطلبٍ صريح من الطالب، ولا يُحفظ إلا بطاقةُ اللعبة ومستندها وصورتها، وقشرةُ
الصفحة تُقرأ **من الشبكة أولاً** والمخبّأ بديلٌ عند انقطاعها — فلا تعود مشكلةُ
الواجهة القديمة التي كُتبت تلك القاعدة لتفاديها.

الحدود العملية: ٢ ميغابايت للعبة، و٦٠ لعبة للمعلّم، وعدّادا الزيارة والتقييم
يمنعان التكرار بالعنوان لساعة — يضبطان النقر العابر لا المتلاعب المصرّ، ولا
نبني عليهما علامةً ولا قراراً.

### الافتراضي: نشاطٌ حرّ 🏃

الطالب يفتح الرابط فيجد السؤال الأول فوراً — بلا قاعة انتظار وبلا مدرب ينقل
الشرائح — وبمجرّد أن يجيب ينتقل إلى التالي بلا ضغطة زرّ.

هذا هو الافتراضي لأنه أكثر استعمالات المنصة: واجبٌ يُرسَل برابط، لا صفٌّ أمام
بروجكتر. ومن أراد أن يقود الشرائح بنفسه يختار «المدرب ينقل الأسئلة».

**والمهلة قبل الانتقال ليست تأخيراً بلا سبب**: هي اللحظة التي يقرأ فيها الطالب
«صحيحة» وشرحها — الفائدة التربوية كلها في تلك الثانيتين. فإن كان كشف الإجابة
مطفأً لم يعد لها معنى فتقصر إلى ٧٠٠ms، وأي نقرة على «التالي» تُلغيها فوراً،
وشريطٌ متقلّص يُري الطالب متى ينتقل فلا يفاجئه. والسؤال الأخير بلا انتقال
تلقائي: إنهاء النشاط قرارٌ يتّخذه بيده.

ويستطيع المعلّم إطفاء «الانتقال التلقائي بعد الإجابة» ليبقى الطالب على نتيجته
حتى يضغط بنفسه. وفي الحالتين يرى الطالب **شريط انتقالٍ ملتصقاً بأسفل شاشته**
يقول إلى أين يذهب («السؤال التالي — ٢ من ٥») ويظلّ في متناول إبهامه مهما مرّر.
كان زرّاً في أعلى الصفحة فوق خبر «إجابة صحيحة» نفسه، تتساقط عليه قصاصات
الاحتفال فيبهت، ثم يغيب عن النظر عند أول تمرير لقراءة الشرح.




### رابط الواجب: موعد التسليم 📅

المنصة تصف نفسها بأن أكثر استعمالاتها **«واجبٌ يُرسَل برابط»**، ثم كانت تُسقط الجلسة بعد ثلاث
ساعات خمول. فمعلّمٌ يرسل واجب نهاية الأسبوع ليل الخميس يجد رابطه ميتاً صباح السبت، وطلابه يظنّون
أنهم أخطؤوا.

في إعدادات النشاط: **آخر موعد للتسليم** (ومعه اختصارات «غداً · بعد ٣ أيام · بعد أسبوع» لأن
كتابة التاريخ بالتقويم في كل مرة عناء).

| | مدة الاختبار | موعد التسليم |
| --- | --- | --- |
| نوعه | مهلة **نسبية** تبدأ من انطلاق الجلسة | موعد **مطلق** لا علاقة له بمتى فتح أولُ طالبٍ الرابط |
| يعني | «أمامكم ٤٥ دقيقة» | «سلّموا قبل الأحد» |
| يصلح لـ | حصةٌ في قاعة | واجبٌ يُرسَل برابط |

وحين يجتمعان يُقفل **أقربهما** — من وضع الاثنين يقصد «ساعةٌ لكل طالب، على ألّا يتجاوز أحدٌ
الأحد»، لا أن يُلغي أحدهما الآخر.

وثلاثة سلوكيات تجعله واجباً حقيقياً:

- **المنظّف لا يمسّه قبل موعده** مهما طال الخمول بينهما — وهذا هو بيت القصيد.
- **يُقفل نفسه في موعده** كما لو ضغط المعلّم «إنهاء للجميع».
- **موعدٌ مضى أثناء توقّف الخادم يُقفل الواجب فور إحيائه** — إحياؤه مفتوحاً يعني قبول تسليماتٍ
  متأخرة بلا علم المعلّم، وهو أسوأ من ضياع الجلسة.

ويراه الطالب **على شاشة الدخول** لا في قاعة الانتظار وحدها: الواجب افتراضُه «الوضع الحرّ + بدء
تلقائي»، فالطالب لا يمرّ بقاعة انتظارٍ إطلاقاً — يكتب اسمه فيجد السؤال الأول.

### الفصول وكشف الأسماء 🧑‍🏫

من «نشاطاتي» يستطيع المعلّم — **اختيارياً بالكامل** — أن يكتب كشف أسماء صفّه، ثم يُرفقه بأي
نشاط. غرضه اثنان لا ثالث لهما:

1. **يختار الطالب اسمه من قائمة** بدل كتابته، فتتّحد كتابته بين الحصص — لا «احمد» و«أحمد»
   صفَّين في التقرير.
2. **يرى المعلّم من لم يدخل بعد** بأسمائهم في لوحة التقدّم، بدل أن يعدّ الظاهرين بإصبعه
   ويقارنهم بدفتره.

> **والوعد الأصلي لم يُمسّ.** هذه قائمةٌ كتبها المعلّم في حسابه، لا بياناتٌ جمعتها المنصة من
> الطلاب، ولا تُربط بإجابة أحد: لا بريد ولا رقم ولا درجة تُحفظ هنا. وإجابات الطلاب تبقى في
> الذاكرة وتختفي بانتهاء الجلسة كما كانت.

وقراران يفصلان كشفاً يُستعمل عن كشفٍ يُزعج:

- **الكشف دليلٌ لا بوّابة**: «اسمي غير موجود» يفتح الكتابة الحرّة. طالبٌ جديد أو زائر لا يجوز
  أن يُمنع من الحصة لأن قائمةً لم تُحدَّث.
- **الإرفاق نسخٌ لا إشارة**: تُنسخ الأسماء في إعدادات النشاط. ولو أشرنا إلى الفصل بمعرّفه لتغيّر
  كشف نشاطٍ أُطلق الأسبوع الماضي بمجرّد أن يعدّل المعلّم فصله اليوم — والنشاط سجلٌّ لما كان لا
  لما صار.

والأسماء تُقبل ملصوقةً كما هي — أسطراً أو فواصل عربية أو إنجليزية — وتُنظَّف من الفراغات
والتكرار والفارغ.


### السِمَة الداكنة في كل المنصة 🌙

زرُّ تبديلٍ في كل صفحة (بجوار زرّ اللغة)، وترتيبُ الأولوية واحدٌ في كل موضع:

**اختيار الزائر الصريح** ← **افتراض الصفحة** ← **إعداد نظامه**.

والاختيار يُخزَّن في `tapio:theme` فيثبت بين الصفحات والزيارات. ويُحمَّل `theme.js` في `<head>`
قبل أي رسم عمداً: لو انتظر نهاية الصفحة لَومَض الأبيض في وجه من اختار الداكنة.

| الشاشة | افتراضها | قابلة للقلب؟ |
| --- | --- | --- |
| الرئيسية · الدخول · الدليل · المكتبة · الألعاب · الخصوصية · الشروط | فاتحة (أو إعداد النظام) | ✅ |
| المحرّر · نشاطاتي · المساعد · بروفايلي | فاتحة | ✅ |
| **الجلسة المباشرة** في لوحة المعلّم | **داكنة** | ✅ |
| **شاشة الطالب** و**شاشة البروجكتر** | **داكنة** | ✅ |
| صفحة «بلا إنترنت» | تتبع النظام | تلقائياً (بلا زرّ — لا شبكة تحمل قاموساً) |

الشاشات الثلاث الأخيرة داكنة **افتراضاً** لأن ألوان الخيارات وبطاقة النتيجة مبنيّة على أرضيةٍ
داكنة، وقاعةٌ مضاءة لا تُقرأ فيها شاشةٌ بيضاء. لكنها **ليست مفروضة**: من يقرأ تحت الشمس أو يجد
الداكنة متعبةً له يقلبها، واختيارُه يعلو على الافتراض.

> وكانت الجلسة المباشرة تُفرَض بصنف `stage` على `<html>` **يتجاهل اختيار المعلّم** — فمن اختار
> الداكنة يجد محرّره أبيض في كل مرة ولا يفهم لماذا لا يعمل زرّه. صار الصنف افتراضاً
> (`data-theme-default`) يحسمه `Theme` لا يفرضه.

**مقيسٌ لا مفترَض**: `verify-dark` يزور عشر صفحات في السِمَتين، ويقرأ اللون الذي **يرسمه
المتصفّح فعلاً**، ويحسب تباين WCAG بين كل نصٍّ وأرضيّته — مُركِّباً الطبقات نصف الشفّافة بعضها
فوق بعض، وإلا قُرئت `rgba(164,92,0,0.1)` لوناً معتماً فصرخ الفحص على تحذيرٍ سليم. ٨٢ فحصاً.

### البداية الموجّهة 🚀

المعلّم الذي يدخل أول مرة يجد في «نشاطاتي» بطاقة **أربع خطوات**، وليست جولةً تعليمية تُعرض ثم
تُنسى: كل خطوة **تُقاس من حالة حسابه** الحقيقية فتُشطب وحدها حين تتم.

| الخطوة | متى تُشطب |
| --- | --- |
| حسابك جاهز | فور دخوله |
| صمّم نشاطك الأول | أول نشاط محفوظ |
| أطلقه وشارك الرمز | أول جلسة يستضيفها |
| تابع النتائج | أول مرة يفتح فيها لوحة التقدّم |

والشرح والأزرار تظهر **للخطوة الجارية وحدها** — أربعةُ شروحٍ معاً تصير جداراً من نصّ. وتختفي
البطاقة كلها عند اكتمال الأربع، ومن يعرف طريقه يُخفيها بضغطة فلا تلاحقه.

> وفي أثنائها أُصلح عطلٌ في التحية: `firstName` كانت تأخذ أول كلمة، فتحيّي «أ. سلمى» بـ«أهلاً
> أ.». والبروفايل نفسه يدعو المعلّم إلى كتابة «أ. فلان»، فالحالة هي القاعدة لا الشذوذ — فصارت
> تتخطّى الألقاب (أ. د. م. الأستاذة… Mr Dr Prof).

### النسخة الورقية 🖨

من صفحة «المراجعة» في المحرّر، أو من زرّ 🖨 على أي نشاط محفوظ. ليست تراجعاً عن المنصة بل
اعترافٌ بالواقع: صفٌّ بلا جوالات، أو امتحانٌ رسمي يشترط الورق، أو طالبٌ غاب. والمعلّم الذي لا
يجدها هنا يعيد كتابة أسئلته في Word — فيصير عنده نسختان تفترقان مع أول تعديل.

تخرج **ورقتان** من المصدر نفسه:

1. **ورقة الطالب**: خانات الاسم والصف والتاريخ، ومربّعات تُعلَّم بالقلم للاختيارات، وخانات أرقام
   لسؤال «رتّب»، وخانات حروف لـ«طابِق»، وأسطر منقّطة للإجابات الحرّة، ومقياسٌ بدوائر مرقّمة.
   الشرائح تُطبع ملاحظاتٍ بلا رقم ولا مكان إجابة.
2. **مفتاح الإجابات** بعد فاصل طباعة (`break-before: page`) — صفحةٌ مستقلة لا تُوزَّع سهواً مع
   الأسئلة، فيها الإجابة وشرحها، و«يُصحَّح يدوياً» لما لا يُصحَّح آلياً.

تفاصيل تُهمّ من جرّبها:

- **العلامة تُقسم على ما يحمل علامةً وحده** — بنفس قاعدة المحرّر والخادم. لو دخل استطلاعُ رأيٍ
  في القسمة لصار مجموع الورقة أقلّ من علامتها الكاملة.
- **«رتّب» و«طابِق» يُخلطان** — طباعتهما بترتيب الإدخال تُفشي الجواب. والخلط **مُبذَّر بمعرّف
  السؤال** لا عشوائي: المعلّم يطبع الورقة مرتين، فلو اختلف الترتيب بينهما لصار المفتاح لورقةٍ
  أخرى. وإن وقع الخلط على الترتيب الأصلي نفسه (احتماله ١/٦ بثلاثة عناصر، وهو ثابتٌ لا عابر لأن
  البذرة ثابتة) دُوِّر دورةً واحدة.
- **مفتاح «طابِق» يعطي الحرف** الذي على الورقة لا القيمة وحدها: الطالب كتب حرفاً في الخانة،
  و«١ ← Fe» تُجبر المعلّم على البحث عن حرف Fe في كل ورقة.

### وضعان لا ثلاثة: و«التلقائي» مفتاحٌ لا خيار ⏱️

كان بجوار «الحرّ» و«المدرّب» وضعٌ ثالث اسمه «⏱️ تلقائي»، وهو في حقيقته
**المدرّب نفسه** وقد سلّم زرّ الانتقال إلى مؤقّت: كل ما عداه متطابق. فصار
مفتاحاً — «انتقال تلقائي بين الأسئلة» — لا يظهر إلا بعد اختيار «المدرّب»،
مطفأً افتراضياً، وتشغيلُه يكشف مدّة بقاء النتائج. والقيمة المخزَّنة تبقى
`pace: 'auto'` كما كانت، فالأنشطة القديمة تعمل بلا تعديل وتعود إلى المحرّر
بمفتاحها مُشغّلاً.

### نظام التقييم: اختيارٌ واحد لا خليط 🏅

المعلّم يختار **غرضه** من الإعدادات، فتعرض المنصة لغة ذلك الغرض وحدها:

| الوضع | ما يراه الطالب | لمن |
| --- | --- | --- |
| 🎮 **نقاط ومنافسة** | نقاط وسلسلة ولوحة ترتيب وأوسمة | المسابقات وكسر الجليد |

> **ولا مضاعف سلسلة.** كان يضرب نقاط الإجابة حتى ‎×١٫٥‎ بحسب صحيحاتٍ سابقة، فيرى الطالب
> «+١٥٠٠» على سؤالٍ قيمته ألف ولا يعرف من أين جاء الفرق، ويرى المعلّم ترتيباً لا يستطيع شرحه
> لوليّ أمر. والسلسلة تبقى شارةَ حماسٍ 🔥 ووسامَ «أطول سلسلة» — تحفيزٌ بلا رياضياتٍ خفيّة.
> والنشاط القديم الذي حُفظ فيه `streakBonus: true` يمرّ بلا أثر، مثبَّتٌ باختبار.
| 📝 **علامات ودرجات** | علامة من مجموع، وتقدير، ونسبة نجاح | الاختبارات الرسمية |
| 🕊️ **بلا تقييم** | لا شيء من ذلك | الاستطلاعات والعصف الذهني |

الجمع بينها كان الخطأ الأول في التصميم: طالبٌ يرى «٤٦٠٠ نقطة» و«٢٤ من ٣٠» معاً لا يعرف
أيّهما نتيجته، ومعلّمٌ يرى العمودين لا يعرف أيّهما ينقل إلى دفتره. فصار الاختيار واحداً،
والحقول التابعة تُشتقّ منه على الخادم لا تُترك للعميل: وضع العلامات يفرض النقاط الثابتة
(وإلا تجاوزت العلامة سقفها)، ووضع النقاط يُسقط العلامة الكاملة.
والنشاط القديم الذي لا يعرف الحقل يُستنتج وضعه من إعداداته فلا ينكسر شيء.

### العلامة: «هذا الاختبار من ٣٠» 📝

العلامة **ليست النقاط**، وهذا الفصل هو كلّ التصميم:

| | النقاط | العلامة |
| --- | --- | --- |
| ما هي | لعبة | سجلّ |
| تكافئ | السرعة | الإتقان وحده |
| تحرّك | لوحة الترتيب والحماس | دفتر المعلّم وتقرير الإدارة |
| سقفها | نقاط السؤال | العلامة الكاملة، لا تتجاوزها أبداً |

لو حُسبت العلامة من النقاط لصار طالبان أجابا الإجابات نفسها يخرجان بعلامتين
مختلفتين لأن أحدهما أسرع إصبعاً — وهذا ظلم لا يُحتمل في وثيقة تذهب إلى الأهل.
لذلك يخزّن كل جواب **نسبة إتقانه** (`merit`، من ٠ إلى ١) مستقلّةً عن الزمن،
والعلامة = Σ(الإتقان × علامة السؤال) ÷ Σ(علامات الأسئلة) × العلامة
الكاملة. مثبَّت باختبار يقيس أن السريع والبطيء يخرجان بالعلامة نفسها بينما
تختلف نقاطهما.

قواعد أخرى تحكمه:

- **ما يدخل في العلامة**: كل سؤال مصحَّح علامته > 0، موزوناً بعلامته. الاستطلاع
  وسحابة الكلمات والمقياس والشرائح خارجها — لا صواب فيها فلا يُحاسَب عليها أحد.
- **السؤال الذي لم يُصحَّح بعد يُستثنى من البسط والمقام معاً**، فلا تُقرأ علامةٌ
  ناقصة على أنها ضعف، ويرى الطالب تنبيهاً بأنها قد تتغيّر.
- **التقدير الأدنى «يحتاج مراجعة 💪» لا «ضعيف»**: الطالب يقرأ هذا وحده على
  هاتفه، وكلمةٌ تُحبطه لا تُصلح إجابةً واحدة. والرقم صريح على أي حال.
- **نسبة النجاح** يضبطها المعلّم، وعليها يُبنى «ناجح/دون النجاح» وعدّاد الناجحين
  ومتوسط الصفّ في اللوحة وفي التقارير الثلاثة (Excel وPDF المباشر والملوّن).
- **بطاقة النتيجة** تحمل العلامة أيضاً: هي ما يُري الطالبُ أهلَه فعلاً، لا نقاطَ
  لعبةٍ لا يعرفون معناها.

**ولماذا لا توجد «مستويات» تتراكم عبر الأنشطة؟** لأن ذلك يعني ملفّاً دائماً لكل
طالب على خوادمنا. وعد المنصة أن **بيانات الطلاب لا تُحفظ**؛ ومستوىً يتراكم عبر
الحصص ينقض هذا الوعد. فالتحفيز هنا كلّه داخل الجلسة: نقاط وسلسلة وأوسمة وترتيب
وتقدير وبطاقة يحملها الطالب بنفسه.

### نتائج الاستطلاع للمدرب 📊

الاستطلاع لا يُصحَّح، فصفّه في جدول الأداء كله شُرَط «—»: لا دقّة ولا صواب. وما يريده المعلّم
منه سؤال واحد — «ماذا قال الصفّ؟» — فتُجاب عنه لوحةٌ في أعلى لوحة التحكم **بلا نقر**: أعمدة
مرتّبة بالأصوات لا بترتيب المحرّر، الرأي الأعلى مميّز (ولا يُتوَّج أحد عند التعادل)، والنسبة
والعدد وكم أجاب من الصفّ.

والتفصيل خلف زرّ: **من اختار كل رأي، ومن لم يشارك**. وهذه بيانات **للمدرب وحده** —
`withoutVoters` تنزعها من حمولة اللوحة قبل كل إرسال إلى شاشة العرض، فالبروجكتر معلّق أمام
الصفّ كله وأسماء أصحاب الآراء ليست معروضة عليه. مثبَّت بأربعة اختبارات في `test/poll.test.js`.

وفي جدول الأداء صارت خانة الدقّة تسمّي السبب («استطلاع رأي»، «سحابة كلمات»…) بدل شرطة صامتة
تُقرأ كبيانٍ ناقص.

### تفاصيل واجهة تُقاس لا تُذاق 🎛️

- **شريط الطالب سطر واحد**: عنوان النشاط كان يلتفّ ثلاثة أسطر ويدفع الأزرار إلى سطر ثانٍ،
  فيأكل ١٢١px من شاشة جوال في كل سؤال. الآن ٥١px: العنوان `flex: 1 1 0` (يأخذ ما يفضل عن
  الأزرار لا العكس) ويُقصّ بحذفٍ مع تلميح بالعنوان كاملاً.
- **أزرار الإجابة تملأ الشاشة**: كانت تتكوّم في الثلث الأعلى ونصف الشاشة فارغ. هدفٌ أكبر =
  ضغطة أدقّ في اللحظة الوحيدة التي يتنافس فيها الصفّ على الثواني. مقيّدة بشاشة طويلة
  (`min-height: 620px`) كي لا تنضغط القصيرة، ومقيسة على ٦٤٠px و٨٤٤px.
- **`[hidden]` قاطعة**: السمة أضعف تخصيصاً من أي صنف يضبط `display`، و`.stack { display: grid }`
  كان يُظهر لوحاتٍ نبنيها مخفيّة (جواب المراجعة، تفصيل الاستطلاع). قاعدة عامة واحدة أغلقت
  الباب على كل تكرار لهذا الخطأ.
- **حلقة تركيز** واحدة لكل ما يُنقر ويُكتب فيه، بـ `:focus-visible` لا `:focus` فلا تظهر لمن
  ينقر بالفأرة. ومن يدير جلسته من لوحة المفاتيح ما عاد يفقد موضعه.
- **زرّ معطّل يبدو معطّلاً**: الشفافية وحدها لا تكفي على زرّ ملوّن، فأُذهب لونه أيضاً.
- **الجداول**: تخطيط متناوب وأرقامٌ بعرض ثابت — تُقارَن الأعمدة بنظرة لا بقراءة.
- **`prefers-reduced-motion`** محترمة عالمياً، و`scroll-margin` يمنع الشريط الملتصق من ابتلاع
  عنوان القسم عند القفز إليه من فهرس الدليل.

### المكتبة العامة 🌍

معلّم ينشر نشاطه، ومعلّم آخر ينسخه إلى حسابه ويعدّله. النسخة **مستقلة تماماً**: تعديل الأصل
لا يمسّها، وسحب الأصل من المكتبة لا يسحبها من صاحبها — وإلا لصار كل من نسخ رهينةَ قرار غيره.

القرارات التي تحكم التصميم:

- **النشر قرار صريح** لصاحب النشاط، مع تحذير مكتوب بأن الأسئلة **وإجاباتها الصحيحة** ستصير
  مرئية. ولا يُنشر نشاط أقلّ من ثلاثة أسئلة — المكتبة للمحتوى لا للتجارب.
- **الفهرس مفتوح بلا حساب** (أوسع بوابة يدخل منها معلّم جديد)، لكنه **لا يحمل الأسئلة**:
  عناوين وموادّ وصفوف وعدد أسئلة فقط. **فتح الأسئلة يتطلّب حساباً** — المكتبة فيها إجابات
  صحيحة، وفتحها للزائر المجهول يجعلها ورقة غشّ لطالب يبحث عن اختبار غده.
- **الاسم الأول وحده** يُنسب إليه النشاط: نشر درساً لا سيرةً ذاتية.
- **صور الأسئلة** تُسقط عن الناسخ المجاني بإخبارٍ صريح بدل رفض النسخة كلها — منعُ محتوى
  مجّاني بسبب صورةٍ ليست له عقوبةٌ في غير محلّها.
- **الاستنساخ الداخلي لا يرث النشر** ولا عدّاد النسخ، وإلا ظهر النشاط مرّتين في المكتبة.
- **الإبلاغ** عن محتوى مخالف يفتح واتساب الدعم نفسه المذكور في التذييل، والمالك يستطيع سحب
  أي نشاط من المكتبة.

### فيديو يوتيوب داخل السؤال 🎬

حقل واحد في المحرّر يقبل رابط يوتيوب، فيظهر المقطع فوق نصّ السؤال عند الطالب وعلى البروجكتر معاً.
الخطر هنا أن حقلاً حرّاً يُبنى منه `iframe` هو ثغرة حقن جاهزة، فالتعامل معه في طبقتين:

- **الخادم لا يخزّن رابطاً إطلاقاً**، بل يستخرج **معرّف الفيديو وحده** (`cleanVideo`): أحد عشر
  محرفاً من `[A-Za-z0-9_-]`، مقبولاً من `youtube.com/watch` أو `youtu.be` أو `/embed/` أو
  `/shorts/` أو `/live/` أو معرّفاً مجرّداً. أي شيء آخر — نطاق آخر، `javascript:`، طول مخالف —
  يصير `null`.
- **العميل يبني الرابط بنفسه** من ذلك المعرّف مع `encodeURIComponent`، فلا يمرّ نصّ المعلّم كما هو
  إلى `src` بحال.

والتشغيل عبر **`youtube-nocookie.com`**: لا يُنشئ يوتيوب ملفّ تتبّع للطالب قبل أن يضغط تشغيل —
موقف المنصة من بيانات الطلاب يجب أن يصمد حتى حين نستضيف محتوى غيرنا.

### مراجعة الطالب لنفسه بعد النشاط 📚

بعد انتهاء النشاط يفتح الطالب «راجع إجاباتك»: بطاقات سؤالاً سؤالاً، **الأخطاء أولاً** لأنها ما
يحتاج مراجعةً فعلاً، وفي كل بطاقة إجابته والإجابة الصحيحة وشرح المعلّم.

**الكشف بالنقر لا تلقائياً** عن عمد: محاولةُ استرجاع الجواب من الذاكرة قبل رؤيته هي ما يثبّت
التعلّم؛ بطاقة تعرض الجواب جاهزاً تُقرأ ولا تُحفظ.

المراجعة رسالة `review` على المقبس، والخادم **يرفضها ما لم ينتهِ النشاط أو ينتهِ الطالب** —
وإلا لصارت باباً خلفياً لتسريب الإجابات أثناء الاختبار. والبيانات تُبنى من حالة الجلسة في الذاكرة
(`Session.reviewFor`)، فالشرائح مستثناة، وإجابات `order`/`match` تُصاغ نصاً مقروءاً
(«ثانياً ← أولاً»)، والسؤال الذي لم يُصحَّح بعد يظهر بعلامة انتظار لا بصفر.

### الهوية والتذييل والصفحات القانونية 🏢

تذييل موحّد في كل صفحة (`public/assets/js/footer.js`) — مكان واحد بدل تكراره في ست ملفات:
هوية **makeflow** ورابطها، وواتساب الدعم والشكاوى، وروابط **سياسة الخصوصية** و**شروط الاستخدام**،
وزرّ تثبيت التطبيق. لا يظهر على شاشة الطالب ولا البروجكتر عمداً (تشويش أثناء الحصة).

الصفحتان القانونيتان (`privacy.html` و`terms.html`) محتواهما بيانات ثنائية اللغة في
`assets/js/legal.js`، ومكتوبتان لتصفا ما يفعله التطبيق فعلاً: نتائج الطلاب لا تُحفظ، ولا متتبّعات،
ولا بيع بيانات. **أي تغيير في سلوك التطبيق يجب أن ينعكس فيهما.**

### تثبيت التطبيق على الجوال 📲

زر التثبيت في التذييل يظهر **فقط حين يعرض المتصفح إمكانية التثبيت فعلاً** — لا نَعِد بما لا نملك.
ولكي يعرضها المتصفح لزم شيئان كانا ناقصين:

- **عامل خدمة** (`sw.js`): كروم لا يُطلق `beforeinstallprompt` بلا عامل خدمة يعالج `fetch`.
  عاملُنا **لا يخزّن أي ملف من التطبيق** عمداً — الحالة في ذاكرة الخادم، وأي نسخة مخبّأة قديمة
  تعني معلّماً يرى واجهة لا تطابق خادمها بعد النشر. يمرّر كل طلب إلى الشبكة، ويحتفظ فقط
  بصفحة اعتذار (`offline.html`) تُعرض حين ينقطع الاتصال.
- **أيقونات PNG** بمقاسي 192 و512 (ومنها maskable لأندرويد) في المانيفست.

على iOS لا يوجد `beforeinstallprompt` إطلاقاً، فيشرح الزر خطوة «إضافة إلى الشاشة الرئيسية» بدل
أن يكون زراً لا يفعل شيئاً.

### الأوسمة والترتيب: النشاط المُقيَّم وحده 🏅

`Session.isAssessed` تحسم: نشاطٌ فيه سؤال مصحَّح آلياً أو سؤال يدوي بعلامة، وإعداد النقاط ليس
«بلا نقاط». وما عداه — استطلاع الرأي وسحابة الكلمات والمقياس — **لا أوسمة فيه ولا ترتيب**:
فالمستطلَع يُشكَر لا يُقيَّم، ووسام «الأسرع» أو مركزٌ بين المشاركين يوحيان بتفوّق في نشاط لا فائز فيه.
مغطّى بخمسة اختبارات في `test/badges.test.js`.

### كم صفاً يتحمّل الخادم؟ (مقيس لا مُقدَّر) 📊

قِسنا الحِمل على خادم مقيّد بـ**0.1 معالج** — حصة خطة Render المجانية بالضبط — عبر مجموعة
تحكّم (cgroup)، والعملاء من خارج القيد:

| الصفوف المتزامنة | الطلاب | المقابس | زمن انضمام الجميع | زمن استجابة الطالب (p95) | ذاكرة |
| --- | --- | --- | --- | --- | --- |
| ١٠ | ٢٥٠ | ٢٧٠ | ٢٫٢ ث | ٥١ms | ٨٩MB |
| ١٥ | ٣٧٥ | ٤٠٥ | ٣٫٥ ث | ٥١ms | ٩٢MB |
| ٢٥ | ٦٢٥ | ٦٧٥ | ٥٫١ ث | ٥٢ms | ١١٢MB |
| ٤٠ | ١٠٠٠ | ١٠٨٠ | ٧٫٧ ث | ٥١ms | — |

**بلا فقدان إجابة واحدة** في أي مستوى، وبلا أي تسرّب بين الصفوف: لا مشارك يظهر في صفٍّ غير صفّه،
ولا لوحة معلّم تعرض طلاب غيره، ونقاط كل طالب صحيحة إلى آخر نقطة.

الشيء الوحيد الذي يتأثّر بالحمل هو **لحظة الانضمام الجماعي** (مصافحة المقابس)، وحتى عند ألف طالب
يدخلون في اللحظة نفسها لا تتجاوز ثماني ثوانٍ. أما زمن استجابة الإجابة فثابت لا يكاد يتغيّر.

الذاكرة: الخطة المجانية ٥١٢MB، والاستهلاك يبقى تحت ١٢٠MB حتى عند ٦٢٥ طالباً. وحدّ جسم الطلب
يمنع أي نشاط واحد من تجاوز ١٢MB مهما بلغت صوره.

### الإغلاق المرتّب عند النشر 🔌

`server.close()` وحده **لا يكفي**: فهو ينتظر كل اتصال مفتوح، ومقابس الويب مفتوحة عمداً طوال
الحصة. فما دام مدرب أو بروجكتر أو طالب متصلاً، لا يُستدعى ردّ الإغلاق أبداً، وتقتل المنصة
العمليةَ قسراً بعد مهلتها — فيرى الصفّ انقطاعاً غامضاً.

لذلك يغلق الخادم مقابس الويب أولاً برمز **1001** («الخادم يُغلق») كي يعرف عميلُ كل طالب أن
يُعيد الاتصال، ثم يُغلق الخادم، ومعه مهلة خمس ثوانٍ تضمن الخروج مهما تعثّر اتصال.

هذا السلوك مغطّى بثلاثة اختبارات في `test/shutdown.test.js`، أحدها يُثبت أن المعالج القديم
يعلق فعلاً — فإن صار الخروج تلقائياً في نسخة عقدة قادمة، سيُنبّهك ذلك الاختبار بالفشل.

---

## كيف يعمل؟

### للمدرب
1. افتح `/host.html` → اختر طريقك: **بالمساعد الذكي** (`#/ai`) أو **يدوياً** (`#/new`) → ابنِ النشاط (أو ابدأ من قالب جاهز).
2. اضغط **ابدأ الجلسة** → يظهر رمز QR + رمز من ٦ أرقام.
3. تحكّم بالعرض: إغلاق الإجابة، كشف النتائج، لوحة الترتيب، السؤال التالي.
4. تابع **لوحة التحكم**: نسبة التفاعل، دقة كل سؤال، تقدّم كل مشارك، متوسط زمن الإجابة.
5. عند الانتهاء يمكنك **تنزيل النتائج** كملف JSON إن أردت الاحتفاظ بها (الخادم لا يحفظ شيئاً).

> مفتاح المضيف (`hostToken`) يُحفظ في `localStorage` على متصفح المدرب فقط. من يملكه يتحكم بالجلسة — لذلك لا يمكن استئناف جلسة من جهاز آخر.

### من ينقل إلى السؤال التالي؟ (يُختار عند الإنشاء)

| الوضع | ماذا يحدث | متى تستخدمه |
| --- | --- | --- |
| 🎛️ **المدرب** | أنت تنقل الشرائح وتتحكم بالإيقاع (الافتراضي) | الشرح والمناقشة بين الأسئلة |
| ⏱️ **تلقائي** | الجميع معاً، وينتقل وحده بعد عرض النتائج بمدة تحدّدها (٣–١٥ ثانية) | مسابقة سريعة بلا توقف |
| 🏃 **حر** | كل متدرب يتقدّم بسرعته: يجيب، يرى نتيجته، ويضغط «التالي» | واجب فردي أو استبيان أو صفوف كبيرة |

في الوضع الحر يرى المدرب لوحة مباشرة: أين وصل كل متدرب (`٢/٣`)، من أنهى، ونتائج أي سؤال بالتصفّح بينها.
ولكل متدرب مؤقّته الخاص يبدأ لحظة فتحه للسؤال، وإن انتهى وقته يتابع إلى التالي بلا عقوبة.

**البدء التلقائي 🚀** (مفعّل افتراضياً في الوضع الحر): ينطلق المتدرب بمجرد دخوله — **لا ينتظر المدرب
إطلاقاً**، لا في البدء ولا في الانتقال. أطفئ الخيار إن أردت أن يبقى الجميع في القاعة حتى تضغط «بدء».

### نظام النقاط والتحفيز

**احتساب النقاط** (يُختار عند الإنشاء):

| الوضع | القاعدة |
| --- | --- |
| ⚡ **بالسرعة** | كاملة للإجابة الفورية وتتناقص حتى نصف القيمة عند آخر ثانية (الافتراضي) |
| 🎯 **ثابتة** | نفس النقاط لكل إجابة صحيحة مهما كانت السرعة — أعدل للمتدربين البطيئين في القراءة |
| 🕊️ **بلا نقاط** | تعلّم بلا منافسة: تختفي النقاط والترتيب تماماً |

**علامة كل سؤال:** يضع المدرب العلامة التي يريدها لكل سؤال بحرية (٠ إلى ١٠٠٠٠)، مع أزرار سريعة للقيم الشائعة.
سؤال بعلامة ٠ يبقى في النشاط لكنه بلا رصيد.

**شرح الإجابة الصحيحة 💡** (اختياري): لكل سؤال حقل شرح، وعند تفعيل «إظهار الإجابة الصحيحة» في إعدادات
النشاط يرى المتدرب — بعد إجابته فقط — الإجابة الصحيحة وسببها. الشرح **لا يُرسل إلى المتصفح إطلاقاً** قبل
أن يجيب، فلا يمكن استخراجه مسبقاً. وإن أطفأ المدرب الخيار فلا يُكشف شيء قبل عرض النتائج للجميع.

**مضاعف السلاسل 🔥** (اختياري): كل إجابة صحيحة متتالية تزيد النقاط ١٠٪ حتى سقف ٥٠٪ —
فالمتدرب الذي يجيب ٤ صحيحات متتالية يأخذ ×1.3 على الرابعة. يظهر المضاعف للمتدرب فور إجابته.

**الأوسمة 🏅** تُحسب في النهاية من الأداء الفعلي، وتمنح كل متدرب سبباً للفخر لا يلتقطه الترتيب وحده:

| الوسام | يستحقه |
| --- | --- |
| 🎯 دقة كاملة | أجاب على سؤالين مصحّحين فأكثر وكلها صحيحة |
| ⚡ الأسرع إجابةً | أقل متوسط زمن بين من أجابوا على نصف الأسئلة فأكثر |
| 🔥 أطول سلسلة | صاحب أطول سلسلة صحيحة (٣ فأكثر) |
| 💪 أجاب على كل الأسئلة | لم يفوّت سؤالاً واحداً |
| 🚀 أسرع من بدأ | الأكثر سبقاً إلى الإجابة قبل الجميع |

**الصعود والهبوط:** بين الأسئلة يرى المتدرب «⬆ صعدت ٣ مراكز» أو «⬇ نزلت مركزاً».

### التنقّل

- **المتدرب:** زر 🚪 للخروج من النشاط (بتأكيد أثناء الجلسة)، ثم يعود للصفحة الرئيسية.
- **المدرب:** زر 🏠 للصفحة الرئيسية — مغادرة الصفحة **لا تُنهي الجلسة**، وتستطيع استئنافها من
  «استئناف جلستي السابقة». وفي شاشة النهاية أزرار «نشاط جديد» و«الصفحة الرئيسية».
- إنهاء الجلسة أو حذفها يبقى قراراً صريحاً من المدرب (زر الإنهاء 🏁 أو الحذف 🗑).

### شاشة عرض منفصلة للبروجكتر 🖥️

في تبويب «🔗 المشاركة» زر **«افتح شاشة العرض»** يفتح رابطاً بلا أي زر تحكم — مناسب للبروجكتر
أو التلفاز، بينما تتحكم أنت من جوالك. تعرض القاعة (QR والرمز)، السؤال والمؤقّت، النتائج (أعمدة/سحابة
كلمات/مقياس)، ومنصة التتويج، بخط كبير يُرى من بعيد. الأمان بنفس مفتاح المضيف — من يملك الرابط فقط يراها.

### وضع الفرق 🏳️

فعّله في إعدادات النشاط واختر عدد الفرق (٢-٨). كل مشارك يُوزَّع تلقائياً على فريق ملوّن عند الانضمام
(بتوازن مستمر — أصغر فريق عدداً يستقبل التالي)، ونقاط الفريق مجموع نقاط أعضائه. يظهر ترتيب الفرق
بجانب الترتيب الفردي في لوحتي المدرب والمشارك وشاشة العرض. يناسب الصفوف الكبيرة.

### حساب المدرب ونشاطاته المحفوظة 🔐

الحساب اختياري تماماً — كل شيء يعمل بدونه، لكنه يحفظ أنشطتك لتعيد استخدامها.

1. **دخول عبر جوجل فقط** من `/login.html` — بلا كلمات مرور وبلا نموذج تسجيل منفصل.
   بريد جوجل مُتحقَّق منه دائماً من طرف جوجل، فيصعب على شخص واحد فتح عدة حسابات كما كان
   ممكناً ببريد وهمي + كلمة مرور: **شخص واحد = حساب واحد** عملياً.
2. **الحفظ تلقائي**: أي نشاط تطلقه وأنت مسجّل الدخول يُحفظ في حسابك فوراً — لا يحتاج زر حفظ،
   وإعادة إطلاق نفس النشاط تحدّثه بدل تكديس نسخ. (زر «💾 حفظ في حسابي» ما زال موجوداً للحفظ دون إطلاق.)
3. **إنهاء الجلسة أو إيقافها لا يحذف النشاط** — يبقى في حسابك جاهزاً لإعادة الإطلاق متى شئت.
4. صفحة **«📚 نشاطاتي»** (`/host.html#/mine`) تعرض كل أنشطتك مع عدد الأسئلة ووضع التقدّم وتاريخ آخر تعديل، ولكل نشاط:
   - **🚀 إطلاق جلسة** — يفتح جلسة مباشرة جديدة برمز QR.
   - **✏️ فتح وتعديل** — يحمّله في المحرّر.
   - **📄 استنساخ** — نسخة مستقلة تعدّلها دون المساس بالأصل.
   - **🗑 حذف** — نهائياً بعد تأكيد.
   - إن كان له جلسة تعمل الآن تظهر شارة **«مباشر»** مع زر **«↩ العودة للجلسة»**.
5. **🔎 أسئلتي السابقة**: في صفحة «سؤال جديد» بالمعالج حقلُ بحثٍ يمرّ على **كل** ما كتبه المعلّم
   من قبل — أسئلة أنشطته المحفوظة، ومعها اسم النشاط الذي جاء منه كل سؤال — وضغطةٌ تُدرجه في
   النشاط الحالي.

   > حلّ محلّ **بنك الأسئلة**، وهو حذفٌ مقصود: كان البنك مكاناً ثانياً تعيش فيه الأسئلة، وله
   > زرُّ حفظٍ منفصل على كل سؤال يجب أن يتذكّره المعلّم **قبل** أن يعرف أنه سيحتاجه ثانيةً. وأكثر
   > من احتاج سؤالاً قديماً كان قد كتبه في نشاطٍ سابق لا في بنك. فذهب المفهوم وبقي الغرض. ومن
   > كان له بنكٌ قديم لم يفقده: البحث يمرّ عليه أيضاً.

**الأمان:** لا كلمات مرور مخزَّنة إطلاقاً — الهوية تُثبَت عبر تدفّق OAuth القياسي مع جوجل
(Authorization Code) بحماية CSRF عبر معامل `state` في كوكي قصير العمر. جلسة الدخول عبر كوكي
`HttpOnly` + `SameSite=Lax` (و`Secure` على https). ولا يستطيع مدرب رؤية أنشطة غيره أو تعديلها
أو إطلاقها — الملكية مفحوصة على الخادم في كل مسار.

**المحفوظ:** البريد والاسم من جوجل، وعنوان النشاط وإعداداته وأسئلته. **غير المحفوظ:** أي كلمة مرور،
وأسماء الطلاب، وإجاباتهم، ونقاطهم، وأي نتيجة.

#### تفعيل الدخول عبر جوجل

1. **Google Cloud Console** ← أنشئ مشروعاً (أو استخدم موجوداً) ← **APIs & Services ← OAuth consent screen**:
   اختر **External**، واملأ اسم التطبيق وبريد الدعم — لا حاجة لمراجعة جوجل لعدد مستخدمين صغير (Testing/غير منشور يكفي مبدئياً، أو انشره لاحقاً لإزالة حد الـ 100 مستخدم).
2. **APIs & Services ← Credentials ← Create Credentials ← OAuth client ID** ← النوع **Web application**.
3. تحت **Authorized redirect URIs** أضف رابط النطاق الذي تنشر عليه، بالضبط:
   ```
   https://<نطاقك>/api/auth/google/callback
   ```
   أضف صفاً لكل نطاق تستخدمه فعلياً (رابط Render، ونطاقك المخصّص إن أضفته لاحقاً، و`http://localhost:3000/api/auth/google/callback` للتطوير المحلي إن رغبت).
4. انسخ **Client ID** و**Client secret**، وضعهما متغيّرَي بيئة على الخادم:
   ```
   GOOGLE_CLIENT_ID=...
   GOOGLE_CLIENT_SECRET=...
   ```
5. أعد النشر. تحقّق من `/api/health` — يجب أن ترى `"googleLoginConfigured": true`.

بلا هذين المتغيّرين يبقى التطبيق يعمل بالكامل (الجلسات الحية، الأسئلة، كل شيء) — فقط صفحة
الدخول تُظهر تنبيهاً بأن الحساب غير مُفعّل بعد بدل زر معطّل.

#### تفعيل «صمّم نشاطك بالذكاء الاصطناعي»

المدرب يحاور المساعد في `#/ai`، يفهم منه ما يريد، يصوغ الأسئلة ويعرضها، وعند
الموافقة تُفتح المسودة في المحرّر جاهزة. النموذج يعمل عبر **Azure OpenAI**.

> ⚠️ **المفتاح لا يُكتب في المستودع إطلاقاً.** أضفه متغيّر بيئة على الاستضافة فقط،
> والمتصفح لا يراه لأن كل نداء يمرّ عبر خادمنا (`/api/ai/design`).

| المتغيّر | إلزامي | القيمة |
| --- | --- | --- |
| `AZURE_OPENAI_KEY` | نعم | مفتاح المورد من بوابة أزور |
| `AZURE_OPENAI_ENDPOINT` | لا | افتراضياً نقطة مشروع `makeflow-ai` (واجهة Responses) |
| `AZURE_OPENAI_MODEL` | لا | افتراضياً `gpt-4.1` |
| `AZURE_OPENAI_API_VERSION` | لا | تُضاف فقط لنقاط النشر القديمة `/openai/deployments/...` |

بعد إضافة المفتاح وإعادة النشر تحقّق من `/api/health` — يجب أن ترى `"aiConfigured": true`.
بلا المفتاح يبقى كل شيء يعمل، وتظهر في صفحة المساعد ملاحظة بأنه غير مُفعّل.

الحدود المدمجة: المساعد للمدربين المسجّلين فقط، وبحدّ ٦٠ رسالة لكل مدرب في الساعة،
وبحدّ ٤٠ سؤالاً في المسودة الواحدة.

**كل دخولٍ محادثةٌ جديدة.** المحادثة لا تُحفظ إطلاقاً — تعيش في ذاكرة الصفحة
وحدها. كانت تُحفظ في التخزين المحلي فيعود المعلّم بعد أيام إلى حوارٍ انتهى،
فيبني المساعد على سياقٍ ميّت ويردّ على سؤالٍ لم يعد مطروحاً.

**الافتراضي: أسئلة تُصحَّح آلياً وحدها.** سؤالُ الجواب الحرّ وسؤال أكمل الفراغ
(`open` و`blank` بعلامة > 0) يُلزمان المعلّم بتصحيح كل إجابة بيده بعد الجلسة،
فلا يُفرضان عليه ضمناً. المساعد يصوغ `mc` و`truefalse` و`order` و`match`، وإن
رأى فائدةً في النوعين **سأل المعلّم صراحةً**، وتحت المحادثة خانةٌ يجيب بها.

وهذا **محروسٌ لا مرجوّ**: التعليمات وحدها لا تكفي لأن النموذج يخالف أحياناً،
فـ`dropManual` في `routes-ai.js` تُسقط ما يتسلّل — ولا تحذف بصمت، بل يُذكر في
الردّ عددُ ما أُسقط وكيف يستعيده. واختبارُ المسار يفشل فعليّاً لو عُطّل الحارس.

#### اللغتان: عربي/إنجليزي 🌐

الكشف تلقائي حسب لغة متصفح المستخدم، مع زر تبديل يدوي (EN/AR) يحفظ الاختيار، واتجاه الصفحة
(rtl/ltr) يُضبط قبل أي رسم فلا تظهر ومضة اتجاه خاطئ.

**مترجَم بالكامل:** الصفحة الرئيسية (الدخول بالرمز) · صفحة دخول المدرب · **شاشة المشارك** (كل رحلة
الطالب: الانضمام، الأسئلة بأنواعها السبعة، التغذية الراجعة، الترتيب، بطاقة النتيجة) · **شاشة
البروجكتر** · **لوحة المعلّم** (البثّ المباشر، التصحيح اليدوي، الجدولة، أنشطتي، لوحة
المالك) · **المحرّر** بكل أنواع الأسئلة والقوالب · **مساعد الذكاء الاصطناعي** · **لوحة التحليلات**
والتوصيات · **تقارير PDF وملفات Excel** (بما فيها اتجاه الصفحة وأسماء الأوراق).

القاموس ٧٦٤ مفتاحاً لكل لغة، وتُتحقّق المطابقة بينهما آلياً.

**لغة النشاط:** عند الإطلاق تُخزَّن لغة المعلّم في `settings.lang`، وصفحتا الطالب والبروجكتر
تستدعيان `I18n.setLang(lang, { remember: false })` قبل أول رسم — فتتبعان لغة النشاط دون المساس
بتفضيل الزائر المحفوظ. الأنشطة المُنشأة عبر الـAPI بلا لغة صريحة تبقى على الكشف التلقائي.

**دليل المعلّم** ثنائي اللغة أيضاً: محتواه بيانات مهيكلة في `public/assets/js/help.js`
(الأقسام التسعة بلغتيهما)، تُرسم منها الصفحة كاملة وتُعاد رسمها فوراً عند تبديل اللغة.

#### جدولة الاختبار ومدّته ⏰

في إعدادات النشاط: **موعد فتح الاختبار** (تاريخ وساعة) و**مدة الاختبار بالدقائق**.

- الجلسة تبقى في القاعة حتى الموعد، ويرى الطلاب وشاشة البروجكتر **عدّاداً تنازلياً**، ثم **تبدأ وحدها** بلا أي أمر من المدرب.
- عند انتهاء المدة **يُقفل الاختبار تلقائياً** للجميع وتظهر النتائج، ويظهر أثناءه شريط «ينتهي الاختبار خلال …» للطالب والمعلّم.
- البدء اليدوي المبكر يُلغي مؤقّت الفتح، وتُحسب المدة من لحظة البدء الفعلي.
- الجلسة المجدولة **لا يمسحها منظّف الجلسات** قبل موعدها، فيمكن إطلاقها قبل أيام وتوزيع رمزها.
- الحدود: الموعد حتى ٩٠ يوماً، والمدة حتى ٧٢٠ دقيقة (١٢ ساعة). موعد في الماضي يُعامَل كأنه بلا جدولة.

#### بقاء الجلسات بعد إعادة التشغيل 💾

كانت الجلسات في الذاكرة وحدها، فأي إعادة نشر — أو نومُ الخطة المجانية — تمسح كل رمز موزَّع.
والحالة المؤلمة واقعية: معلّم يجدول اختبار الغد ويوزّع رمزه، ثم يُعاد نشر الخادم ليلاً، فيجد
ثلاثون طالباً صباحاً «الجلسة غير موجودة» بلا سببٍ يفهمونه ولا وسيلة استرداد.

الآن يُكتب **هيكل** كل جلسة على التخزين الدائم (`live_sessions` في Postgres، أو ملف البيانات
محلياً)، ويُقرأ عند الإقلاع **قبل** أن يبدأ الخادم الاستماع — فأول طالب يطرق الرمز يجده حيّاً:

| يُحفَظ | لا يُحفَظ أبداً |
| --- | --- |
| الرمز ورمز المضيف | أسماء المشاركين |
| العنوان والأسئلة والإعدادات | إجاباتهم ودرجاتهم |
| الحالة وموضع العرض والموعد المجدول | ترتيبهم وأوسمتهم |

وقواعدُ الإحياء تحسم الحالات الملتبسة:

- **الجلسة المنتهية لا تُبعث** — نتائجها كانت في الذاكرة وقد ذهبت، وإحياء هيكلها الفارغ يعطي
  المعلّم صفحةً بلا بيانات ويوهمه بعطل. فتُمسح من القرص عند الإنهاء.
- **موعدٌ حلّ أثناء التوقّف** يفتح الاختبار فور الإحياء بدل أن يعلّقه في الانتظار إلى الأبد.
- **جلسة نامت أطول من مهلة الخمول** (٣ ساعات) لا تُبعث، ويعود رمزها متاحاً لغيرها.
- **الأسئلة المخلوطة** تعود بترتيبها المخلوط نفسه، فلا يرى طالبٌ سؤالاً سبق أن أجابه.
- **لوحة المعلّم تصرّح** بأن ما قبل إعادة التشغيل لم يُحفَظ — الصدق أرخص من حيرته أمام عدّاد
  مشاركين صار صفراً.

الكتابة مؤجَّلة ١٫٥ ثانية ومشروطة ببصمة ما يُحفَظ، فإجاباتُ صفٍّ كامل لا تُنتج كتابةً واحدة.

#### تحليل النتائج والتقارير 📈

بعد أي نشاط (وأثناءه) يفتح المعلّم تبويب **«📈 التحليل»** في لوحة الجلسة فيرى:

- **أرقام سريعة**: عدد الطلاب والأسئلة، مدة النشاط، متوسط الصف، الوسيط، نسبة المشاركة.
- **رسوم** (SVG/CSS بلا أي مكتبة خارجية): حلقة متوسط الدرجات، توزيع الدرجات على خمس شرائح،
  دقة الإجابة لكل سؤال، ومتوسط زمن الإجابة لكل سؤال.
- **بطاقات مميّزة**: 🔴 أصعب سؤال · 🟢 أسهل سؤال · ⚡ الأسرع إجابةً · 🐢 الأبطأ · ⏭ الأكثر تخطّياً.
- **جدول الطلاب**: الدرجة والنسبة والترتيب وعدد الصحيح ومتوسط الزمن.
- **توصيات مكتوبة** مبنية على أرقام هذا النشاط تحديداً (لا نصائح عامة): ماذا يعيد شرحه،
  أي سؤال صياغته ملتبسة، من يحتاج متابعة فردية، وهل الوقت كان كافياً.

**الملفات نوعان** (بريميوم)، لكلٍّ جمهوره:

- **سجل علامات الطلاب** — من قسم *تقدم المشاركين* في لوحة التحكم: بطاقة النشاط وجدول العلامات
  مرتباً فقط، بلا أي تحليل. PDF الرسمي فيه سطرا **توقيع للمعلّم والمدير** — تقرير يُقدَّم للإدارة
  أو يُحفظ مرجعاً للعلامات. (`tapio-<الرمز>-results.pdf/.xlsx`)
- **تقرير التحليل الكامل** — من تبويب *التحليل* فقط: بطاقة النشاط، ملخّص التحليل وأبرز الأسئلة،
  رسم توزيع الدرجات، التوصيات، تفصيل كل سؤال، ثم سجل العلامات. Excel التحليل أربع أوراق:
  *بطاقة النشاط*، *الطلاب*، *الأسئلة*، *الإجابات* (كل إجابة في سطر). (`tapio-<الرمز>-report.pdf` و`tapio-<الرمز>.xlsx`)

**لكل تقرير زرّان، لأن للمعلّمين حاجتين مختلفتين:**

| الزر | ما يفعل | متى تستخدمه |
| --- | --- | --- |
| **📄 PDF ملوّن** | يفتح صفحة تقرير كاملة الأنماط ثم نافذة الطباعة؛ تختار «حفظ كـ PDF» | حين يهمّك الشكل: رسوم ملوّنة، دائرة نسبة، أشرطة، بطاقات توصيات |
| **⤓ ملف** | ينزّل ملف PDF فوراً بلا أي نافذة | حين تريد الملف بسرعة، أو تصدّر عشرات التقارير تباعاً |

التقرير الملوّن يستخدم **محرّك الرسوم نفسه** الذي يراه المعلّم في تبويب التحليل (SVG ونصّ حقيقي)،
فيخرج PDF متجهاً: نصّه قابل للتحديد والبحث، ورسومه حادّة عند أي تكبير.

> **مهم:** المتصفحات تحذف ألوان الخلفيات عند الطباعة افتراضاً، فكانت الأشرطة وبطاقات التوصيات
> تخرج بيضاء. يفرض التقرير `print-color-adjust: exact` فتُطبع بألوانها الحقيقية.

أما زرّ **⤓ ملف** فيبني الـPDF في المتصفح عبر jsPDF + autoTable وخط **Amiri** المضمّن
(`public/assets/vendor/`، رخصة SIL OFL) بلا أي اتصال خارجي — جداول مرتّبة بلا رسوم ملوّنة.
تُحمَّل مكتباته كسولاً عند أول ضغطة كي لا تثقل تحميل الصفحة.

#### دليل المعلّم 📖

صفحة `/help.html` (رابطها في الشريط العلوي وفي الصفحة الرئيسية): خطوات أول نشاط، جدول أنواع
الأسئلة ومتى تستخدم كلاً منها، **شرح احتساب النقاط بالتفصيل**، التصحيح اليدوي، أنماط العرض،
قراءة النتائج والتحليل، أفضل الممارسات (تصميم الأسئلة، الوقت، إدارة الصف، ما بعد النشاط)، وأسئلة شائعة.

#### التصحيح اليدوي 📝 (إجابة مفتوحة + أكمل الفراغ)

في المحرّر اختر **«إجابة مفتوحة»** أو **«أكمل الفراغ»** ثم اضبط **علامة السؤال** (٥ مثلاً):

- الطالب يكتب جوابه نصّاً، وتصله رسالة **«وصلت إجابتك — بانتظار تصحيح المدرب»** بلا أي نتيجة.
- في **لوحة التحكم** تظهر للمدرب بطاقة تصحيح لكل سؤال: نص كل إجابة أمامها أزرار العلامات
  من `0` إلى العلامة القصوى (وحقل رقمي إن تجاوزت ١٠) — فيمنح علامة كاملة أو **جزئية (٣ من ٥)** أو صفراً.
- بمجرد الضغط تظهر العلامة للطالب فوراً، وتُضاف إلى مجموعه وترتيبه. وتعديل العلامة لاحقاً يصحّح المجموع تلقائياً.
- **النتيجة النهائية محجوبة**: لو انتهى النشاط وبقيت له إجابة بلا تصحيح يرى «المدرب يصحّح إجابتك الآن»،
  ولا تظهر نقاطه ولا بطاقة نتيجته إلا بعد انتهاء التصحيح.
- العلامة صفر تُبقي السؤال سؤال رأي حرّ كما كان (بلا تصحيح ولا نقاط).

#### أكمل الفراغ ✏️

نوع سؤال جديد: تكتب الجملة وتضع `___` (ثلاث شرطات سفلية) مكان كل فراغ — حتى ٨ فراغات.

- **العلامة تتبع عدد الفراغات تلقائياً** (فراغان ⇐ علامتان) حتى تضبطها بنفسك.
- تستطيع كتابة **الإجابة المتوقعة** لكل فراغ: تظهر لك وحدك في لوحة التصحيح للمقارنة، ولا تصل الطالب.
- الطالب يرى الجملة نفسها وفيها حقول صغيرة يكتب فيها.
- التصحيح **يدوي دائماً** مثل السؤال النصّي: صح / خطأ / علامة جزئية.

#### صورة داخل السؤال 🖼 (بريميوم)

رفع الصور **ميزة بريميوم**: المشترك يرى زر رفع حقيقياً في كل سؤال، وغير المشترك يرى زراً مقفلاً
يشرح الاشتراك عند الضغط. والخادم يرفض إنشاء أو حفظ نشاط فيه صورة من حساب مجاني (٤٠٢) بدل حذفها صامتاً.

الصورة تُصغَّر داخل المتصفح (أقصى ضلع ١٢٨٠ بكسل، وحتى ٦٠٠ كيلوبايت)
قبل حفظها مع السؤال، وتظهر فوق نصّه عند الطالب وعلى شاشة البروجكتور وفي لوحة المدرب.
الصورة تُقدَّم للمشاركين كملف عبر `/api/sessions/:code/questions/:qid/image` (لا كنص داخل كل رسالة)
فتصل مرة واحدة ويخزّنها متصفح كل طالب. لا تُقبل إلا صور مرفوعة (data URL) — لا روابط خارجية.

#### اشتراك بريميوم ولوحة المالك

الميزات المدفوعة: **تصميم النشاط بالذكاء الاصطناعي** و**تصدير النتائج Excel و PDF**.
الاشتراك يدوي: المدرب يتواصل عبر واتساب، والمالك يفعّل حسابه من لوحة المالك.

| المتغيّر | الافتراضي | الوصف |
| --- | --- | --- |
| `ADMIN_EMAILS` | `jihad@makeflow.tech` | بريد المالك (أو عدّة بُرد مفصولة بفاصلة) — من يرى `#/admin` |
| `PREMIUM_WHATSAPP` | `970597034066` | الرقم الظاهر في بطاقة الاشتراك |
| `PREMIUM_PRICE_USD` | `5` | السعر الشهري المعروض |
| `PREMIUM_SIGNUP_TRIAL_DAYS` | `10` | أيام بريميوم تُمنح تلقائياً لحظة إنشاء الحساب — صفرٌ يوقف المنحة |

**لوحة المالك** على `/host.html#/admin` (يظهر زرها 👑 في الشريط العلوي للمالك فقط):
تعرض كل المدربين — الاسم، البريد، تاريخ التسجيل، تاريخ انتهاء البريميوم — مع
أزرار «+ شهر / + سنة / − شهر / 📅 تاريخ محدّد / ✕ إلغاء». التمديد يبدأ من تاريخ
الانتهاء الحالي إن كان سارياً، وإلا من اليوم. المالك مشترك دائماً بلا أن يمنح نفسه شيئاً.

التاريخ محفوظ في عمود `premium_until` في جدول `users` — يُضاف تلقائياً عند أول تشغيل بعد التحديث.

> **حدود التطبيق الحالي:** بوابة المساعد الذكي على الخادم (لا يمكن تجاوزها)، أما
> أزرار التصدير فتُخفى في الواجهة والبيانات نفسها تبقى متاحة لمن يملك مفتاح المضيف
> عبر `/api/sessions/:code/export` (تنزيل JSON المجاني كما كان قبل البريميوم).

#### الربط بـ Supabase (موصى به ومجرَّب) 🟢

Supabase هي Postgres، فالتطبيق يعمل معها **بلا أي تعديل في الكود** — يكفي `DATABASE_URL`.

1. من لوحة Supabase: **Project Settings ← Database ← Connection string**.
2. اختر تبويب **Session pooler** ⚠️ ولا تختر «Direct connection».
3. انسخ الرابط، واستبدل `[YOUR-PASSWORD]` بكلمة مرور قاعدة البيانات.
4. ضعه في متغيّر البيئة `DATABASE_URL` على Render (أو أي منصة).

الجداول (`users`, `activities`, `auth_sessions`) تُنشأ تلقائياً عند أول تشغيل.

> **لماذا Session pooler تحديداً؟** الاتصال المباشر (`db.<ref>.supabase.co`) صار **IPv6 فقط**،
> و Render لا يوفّر IPv6 صادراً — فيفشل الاتصال. الـ Pooler يعمل على IPv4
> (`aws-0-<region>.pooler.supabase.com`). التطبيق يكتشف هذا الخطأ تحديداً ويطبع الحل.
>
> وإن كانت كلمة المرور تحوي رموزاً خاصة فرمِّزها في الرابط (`@` تصبح `%40`).

**للتأكد بعد النشر** افتح `/api/health` وابحث عن:

```json
{ "storage": { "kind": "postgres", "durable": true, "error": null } }
```

لو ظهر `"kind": "file"` فحقل `error` يشرح السبب بالضبط. التطبيق **لا يتوقف** عند فشل قاعدة البيانات —
تبقى الجلسات الحية تعمل، ويتحول تخزين الحسابات إلى ملف مؤقت مع تحذير ظاهر في «نشاطاتي».

مشروع هذا التطبيق على Supabase: **`tafa3l`** (المعرّف `vymuzlxguhwpjxrictac`، منطقة `eu-central-1`)
وجداوله الثلاثة مُنشأة مسبقاً، ومُفعّل عليها RLS بلا سياسات — فلا تُقرأ عبر واجهة Supabase العامة،
والتطبيق يصل إليها باتصال Postgres مباشر لا عبر PostgREST.

**لاختبار الربط قبل النشر** من جهازك:

```bash
DATABASE_URL='postgres://...pooler.supabase.com:5432/postgres' npm run test:pg
```

يشغّل ١٥ اختباراً على قاعدتك الفعلية: البقاء بعد إعادة التشغيل، عزل الملكية، والحسابات كاملة.

#### التخزين الدائم عند النشر

| الإعداد | السلوك |
| --- | --- |
| بلا `DATABASE_URL` | ملف JSON تحت `DATA_DIR` (افتراضياً `./data`) — مناسب للتطوير المحلي وللخوادم ذات قرص دائم |
| مع `DATABASE_URL` | Postgres (تُنشأ الجداول تلقائياً) — **الخيار الصحيح للنشر** |

> على استضافة بقرص مؤقت (مثل خطة Render المجانية) يضيع ملف JSON مع كل نشر. لذلك يعرض التطبيق
> تحذيراً في «نشاطاتي» ويكشف الحالة في `/api/health`. اضبط `DATABASE_URL` لقاعدة Postgres
> (Render Postgres أو Supabase أو Neon) لتبقى الحسابات والأنشطة.

### توليد الأسئلة بالذكاء الاصطناعي ⚡

توليد الأسئلة كله عبر **«صمّم بالذكاء الاصطناعي»** المدمج (تبويب المساعد في لوحة المدرب):
حاوره عن درسك فيصوغ النشاط ويعرضه عليك، وعند موافقتك يفتح في المحرّر جاهزاً.
(بطاقة «حوّل JSON إلى نشاط» القديمة أُزيلت من المحرّر — المساعد المدمج يغني عنها. محلّل JSON
المتسامح `Builder.parseImport` باقٍ داخلياً وهو الذي يفهم مخرجات المساعد نفسه.)

### المتعة والتفاعل
- **عدّاد «استعد ٣٢١»** متزامن على كل الشاشات قبل الأسئلة المؤقتة.
- **تفاعلات فورية:** يرسل المتدرب 👏 🔥 😂 😮 ❤️ 🤔 فتطير على شاشة العرض (بلا نسبتها لأحد، مع حدّ لمنع الإغراق).
- **أصوات** خفيفة مولّدة برمجياً (بلا ملفات) للدخول والتصحيح والعدّ التنازلي، مع زر كتم 🔇.
- **قصاصات ورق** عند الإجابة الصحيحة وعند ختام النشاط، و**منصة تتويج** للأوائل الثلاثة.
- **سلاسل الإجابات** 🔥 شارةً ووساماً — بلا مضاعف نقاطٍ خفيّ (انظر أدناه) — وشدّ في
  الثواني الخمس الأخيرة (نبض صوتي ولون تحذيري).
- **جرّبها بنشاط جاهز — بلا حساب**: زرٌّ في الصفحة الرئيسية يفتح للزائر نشاطاً كاملاً في
  المحرّر يقلّبه ويعدّله. الإطلاق وحده هو ما يطلب الحساب.

> كل المؤثرات تحترم `prefers-reduced-motion`، والصوت مطفأ يبقى مطفأ بين الجلسات.

### للمشارك
1. يمسح رمز QR أو يدخل الرمز في الصفحة الرئيسية.
2. **وضع الاسم:** يكتب اسمه أو كنيته ويحصل على أفاتار SVG عشوائي (يمكنه تغييره بزر النرد).
3. **الوضع المجهول:** إن أطفأ المدرب خيار «طلب الاسم»، يدخل مباشرة بلا اسم ولا تُنسب إجابته لأحد.
4. يجيب، فيرى نتيجته فوراً (صح/خطأ + النقاط) ثم النتائج المجمّعة.

إن انقطع الاتصال يُعاد الوصل تلقائياً وتُستعاد الحالة والنقاط عبر `sessionStorage`.

---

## أنواع الأسئلة

| النوع | الوصف | نقاط؟ |
| --- | --- | --- |
| `mc` | اختيار من متعدد (حتى ٨ خيارات، يدعم إجابات صحيحة متعددة) | ✅ |
| `truefalse` | صح / خطأ | ✅ |
| `poll` | استطلاع رأي بلا إجابة صحيحة | ✖ |
| `scale` | مقياس رقمي (مثل ١..٥) — سلايدر سحب أفقي مع وصف للطرفين | ✖ |
| `word` | سحابة كلمات (كلمة قصيرة تتجمّع حسب التكرار) | ✖ |
| `open` | إجابة نصية مفتوحة | ✖ |

**احتساب النقاط:** الإجابة الصحيحة الفورية تعطي نقاط السؤال كاملة، وتتناقص كلما تأخّرت حتى نصف القيمة عند آخر
ثانية: `points × (0.5 + 0.5 × الوقت المتبقي ÷ المدة)`. بلا مؤقّت تُعطى النقاط كاملة دائماً.

**عدّاد «استعد ٣٢١»:** قبل كل سؤال مؤقّت يظهر عدّاد قصير متزامن مع الخادم على كل الشاشات، ولا يبدأ حساب
الزمن إلا بعد انتهائه — فينطلق الجميع من نفس الخط مهما اختلفت سرعة أجهزتهم. يمكن إطفاؤه من إعدادات النشاط.

---

## البنية

```
server/
  index.js       خادم HTTP (Express) + WebSocket + توليد QR
  session.js     منطق الجلسة: الأسئلة، الإجابات، التصحيح، التجميعات، الإحصاءات
  store.js       مخزن الجلسات في الذاكرة + حفظ هيكلها وإحياؤه + التنظيف الدوري
  templates.js   قوالب جاهزة (اختبار سريع، استطلاع مجهول، تغذية راجعة)
public/
  index.html     الصفحة الرئيسية (إدخال الرمز)
  host.html      لوحة المدرب (محرر + عرض مباشر + إحصاءات)
  play.html      شاشة المشارك
  assets/js/     avatar.js · fx.js · templates.js · common.js · builder.js · host.js · play.js
  assets/css/    app.css
test/
  flow.test.js   اختبار المسار الكامل
```

الواجهة **بلا أي مكتبة أو خطوة بناء**: JavaScript خام + CSS واحد، عربية RTL، تعمل من أول تحميل على الجوال.
رمز QR يُولَّد على الخادم بصيغة SVG، والأفاتار يُرسم في المتصفح بصيغة SVG — بلا صور ولا طلبات خارجية.

---

## واجهة REST

| الطريقة | المسار | الوصف |
| --- | --- | --- |
| `POST` | `/api/sessions` | إنشاء جلسة ← `{ code, hostToken, joinUrl }` |
| `GET` | `/api/sessions/:code` | معلومات عامة (للتحقق من الرمز قبل الدخول) |
| `PUT` | `/api/sessions/:code` | تعديل الأسئلة قبل البدء (يتطلب مفتاح المضيف) |
| `GET` | `/api/sessions/:code/dashboard` | الإحصاءات الكاملة (يتطلب مفتاح المضيف) |
| `GET` | `/api/sessions/:code/export` | تنزيل النتائج JSON (يتطلب مفتاح المضيف) |
| `DELETE` | `/api/sessions/:code` | إنهاء الجلسة ومحو بياناتها فوراً |
| `GET` | `/api/qr?text=…` | رمز QR بصيغة SVG |
| `GET` | `/j/:code` | رابط الدخول المختصر (هدف رمز QR) |

مفتاح المضيف يُمرَّر عبر الترويسة `X-Host-Token` أو المعامل `?hostToken=`.

### رسائل WebSocket (`/ws`)

**من المشارك:** `join` · `rejoin` · `answer` · `next` (الوضع الحر) · `reaction` · `leave`
**من المضيف:** `host:hello` · `host:start` · `host:skip` · `host:prev` · `host:goto` · `host:lock` · `host:results` · `host:leaderboard` · `host:end` · `host:kick` · `host:dashboard`
**من الخادم:** `state` · `joined` · `dashboard` · `reaction` · `answer:accepted` · `answer:rejected` · `session:closed` · `kicked` · `error`

---

## الحدود والحماية

- ٣٠٠ مشارك و٦٠ سؤالاً كحد أقصى لكل جلسة، و٥٠٠ جلسة متزامنة للخادم.
- كل المدخلات تُقصّر وتُنظّف من محارف التحكم على الخادم.
- الإجابة الصحيحة **لا تُرسل** للمشارك قبل كشف النتائج.
- في الوضع المجهول لا يُرسل اسم أو أفاتار مع الإجابات المفتوحة إطلاقاً.
- إجابة واحدة لكل مشارك لكل سؤال، وتُرفض بعد إغلاق السؤال.
- المشارك يُعرَّف بمفتاح سري عشوائي؛ لا يمكن انتحال هوية مشارك آخر.

## الملاحظات

- التطبيق يحتاج WebSocket — خلف بروكسي عكسي فعّل تمرير الترقية (`Upgrade`/`Connection`).
- عند تعدد نسخ الخادم يجب تثبيت الجلسة على نسخة واحدة (الحالة في ذاكرة العملية).
